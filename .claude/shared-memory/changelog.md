# Changelog

Append-only event log. Every agent appends one row on finish.

| Timestamp (UTC) | Agent | Action | Files touched |
|-----------------|-------|--------|---------------|
| 2026-05-25 | main-assistant (bootstrap) | initialize shared-memory directory | INDEX.md, current-task.md, plan.md, decisions.md, blockers.md, api-contracts.md, changelog.md |
| 2026-05-25 | main-assistant (bootstrap) | create agent definitions | .claude/agents/team-leader.md, coder.md, qa.md |
| 2026-05-25 | main-assistant (bootstrap) | write design spec | docs/superpowers/specs/2026-05-25-agent-team-design.md |
| 2026-05-25 | main-assistant (bootstrap) | add sessions/ convention | sessions/2026-05-25-bootstrap.md, INDEX.md |
| 2026-05-26 | main-assistant | fix firebase.json public dir + fix Flutter localhost API URL | anmates_flutter/firebase.json, lib/services/api_client.dart, lib/services/auth_service.dart |
| 2026-05-26 | main-assistant | write R-002 deploy guideline (Firebase Hosting + Cloud Run) | resolutions/R-002-deploy-flutter-firebase-go-cloudrun.md, resolutions/INDEX.md |
| 2026-05-25 | main-assistant | write usage guide | USAGE.md, INDEX.md, sessions/2026-05-25-usage-guide.md |
| 2026-05-25 22:50 | team-leader | diagnose INVALID_APP_CREDENTIAL Firebase Phone Auth; root cause = Firebase Console config (SHA Android / APNs iOS / authorized domains Web); raise BLOCKER-001 for user action | current-task.md, plan.md, blockers.md, sessions/2026-05-25-fix-invalid-app-credential.md |
| 2026-06-04 | main-assistant | onboarding "Hoàn tất" dev bypass — in kDebugMode, swallow submit failure (expired Firebase session / backend hiccup), mark onboarding_done locally + enter app; prod unchanged | anmates_flutter/lib/views/onboarding/photo_upload_view.dart |
| 2026-06-11 | main-assistant | venue gallery: 10-photo swipe (touch+mouse), reachability validation (kills 502s) + Bing relevance filter (wrong-photo venues → count 0 → placeholder); see session 2026-06-11-venue-image-routing | anmates-api/services/venue_image.go, venue_image_test.go, anmates_flutter/lib/views/discover/venue_detail_view.dart |
| 2026-06-11 | main-assistant | category stock fallback for no-real-photo venues + write R-007 (full venue-image pipeline: Bing source, 401/429/404/502 fixes, relevance filter, category fallback, swipe gallery; user-confirmed) | resolutions/R-007-venue-image-bing-relevance-fallback.md, resolutions/INDEX.md, anmates-api/services/venue_image.go, venue_image_test.go |
| 2026-06-11 | main-assistant | Discovery: photo query → name+address, 5-img cap, infinite-scroll to 5km + back-to-top; NEW TomTom nearby-POI proxy (fresher VN data than OSM, server-side key, Overpass fallback when unset); VN map-API advice (TomTom/HERE self-serve free vs Goong phone-gated). Pending TOMTOM_API_KEY + hot restart. See session 2026-06-11-discovery-tomtom-nearby-infinite-scroll | anmates-api/{config/config.go,services/tomtom.go,handlers/venue_nearby.go,main.go,services/venue_image.go}, anmates_flutter/lib/{services/places_service.dart,views/discover/discover_view.dart,views/discover/venue_detail_view.dart}, .env, .env.example |
| 2026-06-11 | main-assistant | fix onboarding photo upload "Phiên đăng nhập Firebase đã hết hạn" — root cause: Email-OTP login never creates a Firebase session so currentUser=null blocks Firebase-Storage upload; fix = signInAnonymously fallback (needs Anonymous provider enabled in Firebase Console). See session 2026-06-11-onboarding-photo-upload-email-otp-firebase-anon | anmates_flutter/lib/services/storage_service.dart |
| 2026-06-11 | main-assistant | TomTom live findings: hard-caps 100 results (~736m, no pagination → 5km impossible) + no photos (poiDetails 403). Footer honest "Đã hiển thị N quán gần bạn"; REMOVED R-007 category-stock fallback (user chose honest per-category placeholder) → crawl returns empty when no venue-matched photo → PhotoSlot placeholder. See session 2026-06-11-discovery-tomtom-nearby-infinite-scroll | anmates-api/services/venue_image.go, venue_image_test.go, anmates_flutter/lib/views/discover/discover_view.dart |
| 2026-06-11 | main-assistant | REVERSED again per user: dropped the no-photo list gate; RESTORED image cascade (real→category F&B fallback, rotated) so every venue always has an image + NSFW/adult blocklist; fixed flaky count:0 (store never caches empties + VENUE_IMAGE_CACHE_TTL=24h). Also: reload venues after location permission granted late on web (_initLocationAndLoad polls + reloads at real coords). See session 2026-06-11-discovery-image-cascade-and-location-reload | anmates-api/services/venue_image.go, venue_image_test.go, anmates_flutter/lib/views/discover/discover_view.dart, .env, .env.example |
| 2026-05-25 23:25 | main-assistant | install gcloud CLI, generate debug keystore, register Android SHA-1/SHA-256 to Firebase, re-download google-services.json, audit phone OTP readiness via Identity Toolkit REST API (Blaze ✅, phone provider ✅, FCM ✅, VN-only SMS region ✅); iOS APNs/entitlements remain user manual | ~/.android/debug.keystore, android/app/google-services.json, sessions/2026-05-25-otp-real-phone-checklist.md |
| 2026-05-26 | main-assistant | RESOLVE BLOCKER-001 (web): user added 127.0.0.1 to Firebase Authorized domains + accesses via http://127.0.0.1:PORT; refactor Flutter per Firebase docs (init in main, RecaptchaVerifier lifecycle, central error mapping, wsUrl from env); flutter analyze clean + build web ✅ | lib/main.dart, lib/views/auth/phone_input_view.dart, lib/views/auth/otp_view.dart, lib/views/auth/auth_error_messages.dart (new), lib/services/api_client.dart, pubspec.yaml, sessions/2026-05-26-firebase-phone-otp-resolved.md, current-task.md, blockers.md |
| 2026-05-26 | main-assistant | introduce `resolutions/` folder (tag + error-keyword indexed, user-confirmed fixes only) + INSTRUCTIONS.md (full directory map + write protocols); promote Firebase phone OTP fix to R-001 | INSTRUCTIONS.md (new), resolutions/INDEX.md (new), resolutions/TEMPLATE.md (new), resolutions/R-001-firebase-phone-otp-web-127001.md (new), INDEX.md (updated), CLAUDE.md (updated) |
| 2026-05-26 | main-assistant | user moved shared-memory from `.claude/agents/shared-memory/` → `.claude/shared-memory/`; created symlinks at project-root (`.claude/agents/` + `.claude/shared-memory/` → AnMatesApp) so canonical path `.claude/shared-memory/...` works from BOTH cwd=root and cwd=AnMatesApp; added project-root `CLAUDE.md` pointer; migrated all path refs in agent files + docs + auto-memory; added "Path migration note" to historical session files | /Users/thanhit/Downloads/AnMates/.claude/{agents,shared-memory} (symlinks), /Users/thanhit/Downloads/AnMates/CLAUDE.md (new), AnMatesApp/CLAUDE.md, AnMatesApp/.claude/agents/{team-leader,coder,qa}.md, INSTRUCTIONS.md, USAGE.md, sessions/2026-05-25-{bootstrap,fix-invalid-app-credential,usage-guide}.md, ~/.claude/projects/.../feedback_persist_session_outputs.md |
| 2026-05-26 | main-assistant | refactor Onboard screen 02 "Chọn quán" to match screenshot: polaroid cards w/ diagonal stripes + custom-painted food illustrations (Lẩu/Cafe/Nướng/Vặt), staggered entry, ambient float, hover lift+snap-upright+glow, press feedback; flutter analyze clean for file; pending visual confirmation | anmates_flutter/lib/views/onboarding/onboarding_view.dart, sessions/2026-05-26-onboard-02-chon-quan-polaroid-cards.md |
| 2026-05-27 | main-assistant | iter 2-3: caption-occlusion fixes (bottom row top:138→220→170 across iterations), enable cross-device swipe via ScrollConfiguration + PointerDeviceKind {touch,mouse,trackpad,stylus}, BouncingScrollPhysics, FittedBox(BoxFit.contain) responsive stage, MediaQuery-driven compact-device sizing in _StepLayout (iPhone SE/11/12/13/14) | anmates_flutter/lib/views/onboarding/onboarding_view.dart |
| 2026-05-27 | main-assistant | iter 4: swap CustomPainter food illustrations for real cartoon PNG assets cropped from user-supplied food_reference.png; write reusable Pillow crop script; wire pubspec assets; delete ~400 lines dead CustomPainter code (_DiagonalStripesPainter, _FoodIllustration, _LauPainter, _CafePainter, _NuongPainter, _VatPainter, _shade, dart:math); flutter pub get + analyze clean | anmates_flutter/assets/food/{lau,cafe,nuong,vat}.png (new), anmates_flutter/pubspec.yaml, anmates_flutter/lib/views/onboarding/onboarding_view.dart, scripts/crop_food_reference.py (new), plan/screenshot/food_reference.png (new) |
| 2026-05-27 | main-assistant | iter 5 (interrupted): user wants to re-source assets from polaroid mockup food_card.png; first-guess crops generated but ambiguity remains (extract food-art-only vs whole-polaroid + refactor Flutter chrome); decision blocked on user clarification | plan/screenshot/food_card.png (new, not yet cropped) |
| 2026-05-27 | main-assistant | TECH-2: setup GitHub Actions CI/CD pipeline — 4 workflows (ci-flutter, ci-go, cd-flutter, cd-go) with path-based triggers, WIF auth (no static SA keys), Firebase Hosting preview channels per PR, Cloud Run smoke check + auto-rollback; bash setup-wif.sh bootstrap script (idempotent) + CI-CD.md docs; YAMLs validated. User action pending: run setup-wif.sh + add GitHub secrets | .github/workflows/{ci-flutter,ci-go,cd-flutter,cd-go}.yml (new), scripts/setup-wif.sh (new), .github/CI-CD.md (new), sessions/2026-05-27-tech-2-github-actions-cicd.md (new) |
| 2026-05-27 | main-assistant | TECH-2 follow-up: adopt workflow naming convention `<lifecycle>.<service>-<platform>.yml`; rename 4 workflows (ci-flutter→ci.flutter-web, ci-go→ci.go-api, cd-flutter→cd.flutter-web, cd-go→cd.go-api) + update internal concurrency groups + path self-refs; write WORKFLOW-ARCHITECTURE.md (Principal DevOps analysis: 5 principles, end-state target, 4-phase migration roadmap); log ADR-001 in decisions.md; update CI-CD.md links; rationale: prepare for iOS/Android workflows without touching existing stable files | .github/workflows/* (renamed + edited), .github/WORKFLOW-ARCHITECTURE.md (new), .github/CI-CD.md (links updated), decisions.md (ADR-001), sessions/2026-05-27-tech-2-github-actions-cicd.md (updated) |
| 2026-05-27 | main-assistant | TECH-2 follow-up: add principle #6 "Version build artifacts by SHA, release artifacts by semver" to WORKFLOW-ARCHITECTURE.md (decided to keep `github.sha` as Cloud Run image tag instead of `v1.<run_number>.<attempt>`); rationale = identifier audience (machine→SHA, human→semver), run_number not idempotent, continuous-deploy backend has no semver, mobile store is where semver belongs; user-driven insight | .github/WORKFLOW-ARCHITECTURE.md (principle 6 added) |
| 2026-05-27 | main-assistant | TECH-2 fix: first CD run failed at image build — `gcloud builds submit` needs caller=Viewer/Owner to stream logs, but least-privilege SA lacks it → exit 1 (false failure, build itself ran). Switched cd.go-api.yml to build on runner via `docker build`+`docker push` (faster, cheaper, fewer perms); removed cloudbuild.builds.editor + storage.admin roles + cloudbuild API from setup-wif.sh; added troubleshooting entry. Error: "This tool can only stream logs if you are Viewer/Owner" | .github/workflows/cd.go-api.yml, scripts/setup-wif.sh, .github/CI-CD.md, sessions/2026-05-27-tech-2-github-actions-cicd.md |
| 2026-05-27 | main-assistant | iter 4: swap CustomPainter food illustrations for real cartoon PNG assets cropped from user-supplied food_reference.png; write reusable Pillow crop script; wire pubspec assets; delete ~400 lines dead CustomPainter code; flutter pub get + analyze clean | anmates_flutter/assets/food/{lau,cafe,nuong,vat}.png (new), anmates_flutter/pubspec.yaml, anmates_flutter/lib/views/onboarding/onboarding_view.dart |
| 2026-05-27 | main-assistant | TECH-2: setup GitHub Actions CI/CD pipeline — 4 workflows with path-based triggers, WIF auth, Firebase Hosting preview channels, Cloud Run smoke check + auto-rollback | .github/workflows/*.yml (new), scripts/setup-wif.sh (new), .github/CI-CD.md (new) |
| 2026-05-27 | main-assistant | TECH-2 follow-up: adopt workflow naming convention; write WORKFLOW-ARCHITECTURE.md; log ADR-001 in decisions.md | .github/workflows/* (renamed), .github/WORKFLOW-ARCHITECTURE.md (new), decisions.md |
| 2026-05-27 | main-assistant | create Jira Epic + 15 Stories for FE-Make-UI-Consistent-With-Design (SCRUM-6 + SCRUM-7 to SCRUM-21) | sessions/2026-05-27-jira-fe-ui-consistency-epic.md |
| 2026-06-03 | main-assistant | author Meetup & Dining Map master plan (18 sections, Mermaid diagrams) — full product+UX+Flutter+backend+QA design for map-based venue selection→booking→meetup; decisions: pragmatic-incremental Riverpod (new features only), flutter_map+OSM for MVP (Mapbox upgrade V2), map becomes core MVP surface; grounded in existing places_service.dart/api_client.dart/locked tokens/pgxpool+PostGIS. Doc-only, no code. Pending user/team review | docs/meetup-map-master-plan.md (new), sessions/2026-06-03-meetup-map-master-plan.md (new) |
| 2026-06-03 | main-assistant | author AI Concierge & Monetization plan (standalone doc, cross-linked from master plan V3). Core mechanic: **triggered/proactive agent** — when a match's Vibe meter (noi_lau_progress) crosses trust threshold 70, AI auto-posts top-3 midpoint venues matching both users' mood+budget into chat. Built on Claude (tool-use loop, Haiku-first + Sonnet escalation, prompt caching, streaming); tools = existing §6/§7/§10 APIs; strict anti-hallucination (venue IDs from tool results only). Monetization: MVP FREE (respects locked Phase 1 no-paywall), Phase 2 sells premium re-roll/refine/"plan whole date" as **consumer quotas, NEVER user-facing token meters** (token budget internal cost-control only). New tables ai_concierge_runs/picks + user_prefs_budget. EPIC-AI-CONCIERGE (8 tickets). User vision in VN confirmed | docs/ai-concierge-plan.md (new), docs/meetup-map-master-plan.md (V3 cross-link), sessions/2026-06-03-meetup-map-master-plan.md |
| 2026-06-03 | main-assistant | ⚠️ CRITICAL pivot: **Google Maps Platform is PROHIBITED in Vietnam** (official Prohibited Territories list — Biển Đông dispute). Reverses prior Google Places decision. New data+routing source = **Goong (goong.io, VN provider, drop-in Google replacement, Places+Directions+Matrix)**; VietMap/Map4D as alternatives; render stays flutter_map+OSM tiles ($0, fine in VN). Amended master-plan §1/§6/§7/§9/§16/§17/§18 + ai-concierge-plan §8/§11 (Google→Goong everywhere). | docs/meetup-map-master-plan.md, docs/ai-concierge-plan.md |
| 2026-06-03 | main-assistant | write SPEC (spec-driven dev) for AI Concierge chat vertical slice. Locked decisions: trigger `points>=70` (keep design's 70, configurable, hook ChatService.IncrementPoints); location via app push → new user_locations + PUT /me/location; venue source = SEED restaurants (~30-50 Q1) for slice, Goong ingest later; Haversine midpoint in Go; Claude Haiku agent behind LLMClient interface (fake for tests, real needs ANTHROPIC_API_KEY); new msg_type `ai_venue_card` authored by reserved AI system user, broadcast via existing Hub; strict anti-hallucination (venue IDs ⊂ candidates). Migrations 006-008. Steps 1-4+6-8 buildable WITHOUT api key (fake LLM); only live E2E needs key. Implementation NOT started yet — awaiting go-ahead | docs/specs/ai-concierge-chat-spec.md (new) |
| 2026-06-03 | main-assistant | IMPLEMENT AI Concierge chat slice (per spec). Backend (Go): migrations 006 restaurants(+18 seed Q1 venues incl. "Bún Bò Giáo Toàn"), 007 user_locations, 008 ai_concierge_runs + AI system user + msg_type 'ai_venue_card'; config AI_* env; PUT /api/v1/me/location (location svc+handler); VenueEngine (Haversine+Midpoint+SearchCandidates); LLMClient interface + FakeLLM + OpenAICompatLLM (LM Studio, json_schema); ConciergeService (gate points>=70, idempotent unique-index, async goroutine, preconditions, anti-hallucination validatePicks, tx persist + hub broadcast to both); IncrementPoints now returns (before,after); chat handler MaybeFire seam; main.go wiring (concierge nil when AI_BASE_URL empty); Go unit tests (threshold/haversine/midpoint/validatePicks/fake/json). Flutter: AiVenueCardContent model + AiVenueCard widget (tokens) + LocationService (geolocator→PUT) + chat_detail_view location push on open + render card (sample, WS seam) + widget test. .env.example + spec updated (host.docker.internal note). ⚠️ go build / flutter analyze NOT run (toolchains not on host PATH — run in Docker/CI). Flutter chat view still a MOCK (WS not wired) — card shown from sample data. | anmates-api/db/migrations/006-008, config/config.go, services/{location,venue,llm,concierge,chat}.go, services/concierge_test.go, handlers/{location,chat}.go, services/interfaces.go, main.go; anmates_flutter/lib/models/ai_venue_card.dart, lib/widgets/ai_venue_card.dart, lib/services/location_service.dart, lib/views/chat/chat_detail_view.dart, test/ai_venue_card_test.dart; .env.example, docs/specs/ai-concierge-chat-spec.md |
| 2026-06-04 | main-assistant | DEV OTP bypass made usable in local Docker build. Root cause: the "Dev Mode (skip OTP)" button in phone_input_view was gated `if (kDebugMode)` → hidden in the Docker `flutter build web --release` (kDebugMode=false). Fix: gate on `_devBypassEnabled = kDebugMode || apiBaseUrl contains localhost/127.0.0.1` (per user: "localhost thì bypass"); added same button + `_devSkipOtp` to otp_view.dart (so user can skip FROM the OTP screen). Backend `/auth/dev-login` already existed (DEV_MODE + DEV_BYPASS_SECRET=dev-local-2026). Verified live: stack up, migrations 006-008 applied, "AI Concierge enabled qwen/qwen3.5-9b trigger 70", dev-login returns JWT (user 6eedaf27..., onboarding_done=false). Appended AI_* to repo-root .env (gitignored). | anmates_flutter/lib/views/auth/phone_input_view.dart, otp_view.dart, .env (local) |
| 2026-06-04 | main-assistant | FIX qwen3.5-9b "empty content": diagnosed via LM Studio — it's a heavy reasoning model; LM Studio routes the JSON answer into `reasoning_content` and leaves `content` empty (json_schema strict gives clean concise JSON in 1.6s, valid ids, CLEAN Vietnamese no-Chinese; json_object → 400; no-format → rambles to token limit). Fix in OpenAICompatLLM: add `reasoning_content` to response struct + fallback `if content empty → reasoning_content` (parseConciergeJSON extracts {...} either way); added max_tokens=2000 headroom. Works for both 9b (reasoning) and vl-7b (content). → 9b now PREFERRED (cleaner VN than 7b). | anmates-api/services/llm.go |
| 2026-06-04 | main-assistant | WIRE Flutter chat ↔ backend WebSocket (live AI Concierge). New `lib/services/chat_socket.dart` (web_socket_channel: connect with ?access_token, parse `{type:message,payload}` envelopes → ApiMessage broadcast stream, sendText, dispose). `chat_detail_view.dart` rewritten: optional matchId/currentUserId → LIVE mode (load getHistory + GET /matches/:id/progress for vibe, connect WS, render real messages incl `ai_venue_card`→AiVenueCard, send over socket, optimistic append); matchId null → demo mode preserved. `chat_list_view.dart`: loads real getConversations() + AuthService.currentUserId() in initState, "ĐANG TÁM" cards navigate live with matchId/currentUserId (mock fallback when none). Removed dead anm_logo import. ⚠️ flutter analyze/test NOT run (no Flutter on host PATH — Docker/CI). Re-test qwen3.5-9b: empty content (likely reasoning-model/json_schema quirk) → recommend vl-7b for now (valid JSON + CJK guard). | anmates_flutter/lib/services/chat_socket.dart (new), lib/views/chat/chat_detail_view.dart, lib/views/chat/chat_list_view.dart |
| 2026-06-03 | main-assistant | smoke-test LM Studio (user's local AI, RTX 5080): server at 127.0.0.1:1234 OK; models qwen/qwen2.5-vl-7b (loaded), qwen3.5-9b, qwen3.6-27b. Tested concierge prompt+json_schema vs vl-7b: ✅ valid JSON, 1.2s, correct anti-hallucination (picked real id), budget filter OK — ⚠️ but `reason` LEAKED CHINESE ("评分高，距离适中"); classic small-Qwen code-switch. HARDENING: (1) system prompt now forbids Hán/Trung/Anh; (2) server-side `safeReason`/`containsCJK` (rune>=0x2E80) strips CJK→Vietnamese fallback in validatePicks; added Go test. Recommend qwen3.5-9b (load in LM Studio first to avoid JIT timeout) for cleaner VN. Dev .env: AI_BASE_URL=http://host.docker.internal:1234/v1 (container→host), AI_MODEL=qwen/<id> with prefix | anmates-api/services/llm.go, services/concierge.go, services/concierge_test.go |
| 2026-06-03 | main-assistant | amend AI Concierge spec: LLM backend = **OpenAI-compatible `LLMClient`**, pluggable via env (`AI_BASE_URL`/`AI_API_KEY`/`AI_MODEL`). **Dev = LM Studio local ($0, no key, e.g. Qwen2.5-7B/14B)**; prod = swap to hosted GPU/cloud endpoint (Cloud Run can't reach localhost). No native tool-calling needed (backend pre-fetches candidates; model does 1 structured-output JSON call). Removed ANTHROPIC_API_KEY hard requirement — now only LM Studio needed for live dev run | docs/specs/ai-concierge-chat-spec.md |
| 2026-06-03 | main-assistant | data-source decision (amend master plan §7/§9/§17/§18): OSM lacks VN long-tail eateries (e.g. "Bún Bò Giáo Toàn") → MVP restaurant data = **Google Places API** server-side ingest (pay-for-quality, ToS-compliant: store place_id, TTL-refresh, no bulk-mirror) + admin curation; OSM = fallback only. REJECTED: Playwright scrape of Google Maps (ToS/legal/brittle/bannable), Bing Maps (retiring 2025–2028), Azure Maps (TomTom weak VN coverage). Render SDK (flutter_map+OSM tiles, $0) kept separate from data provider. User chose "pay for quality coverage" | docs/meetup-map-master-plan.md, sessions/2026-06-03-meetup-map-master-plan.md |
| 2026-05-27 | main-assistant | FE-UI-001 (SCRUM-7): full audit Flutter codebase vs design tokens; 6 Critical/6 Major/4 Minor issues; ~16 days effort estimate | sessions/2026-05-27-fe-ui-001-audit.md |
| 2026-05-27 | main-assistant | FE-UI-007 (SCRUM-13): Screen 03 bg→AppColors.mint, titleColor param on _StepLayout, _Step2Page ocean heading, H1 w700/lh1.2, fix 8x withOpacity→withValues; rebase onto main resolved | lib/views/onboarding/onboarding_view.dart |
| 2026-05-27 | main-assistant | FE-UI-007 iter5 (SCRUM-13): full Screen 03 illustration rewrite — _Step2Illustration 3-card stack + 3 AnimationControllers (entry/float/pulse), _MateProfileCard 196×268, _DiagonalStripesPainter, _InfoChip, AppColors.berryTint token; flutter analyze clean; pushed to feat/fe-ui-007-screen03-social-proof | lib/theme/app_theme.dart, lib/views/onboarding/onboarding_view.dart |
| 2026-05-27 | main-assistant | FE-UI-007: add onboarding Step 4 "Định vị nền" (ETA Maps) — _totalPages 3→4, _Step4Page + _Step4Illustration with 5 AnimationControllers (pin bounce+glow blink, card entry, dashed line draw-in, destination dot pulse), _GlowLocationPin, _EtaMapCard, _MapGridPainter, _DashedRoutePainter, _RouteDot, _RouteLabel; flutter analyze clean (0 issues) | lib/views/onboarding/onboarding_view.dart |
| 2026-05-28 | main-assistant | FE-UI-007 Step 4 location permission flow: redesign _Step4Page to match NEW_ETA_Maps.png — custom top bar (QUYỀN AN TOÀN / Định vị nền / ?), geolocator.checkPermission() check, button 'Bật "Luôn cho phép"' vs 'Bắt đầu', 3 feature bullets (_LocationBullet), full-width CTA; hide PageView logo+dots overlay on last page; _goBack() for ← button; flutter analyze clean (0 issues in onboarding_view) | lib/views/onboarding/onboarding_view.dart |
| 2026-05-28 | main-assistant | FE-UI-007 Step 4 visual-fidelity pass vs NEW_ETA_Maps.png: white-circle ←/? top-bar buttons, centered heading+subtext, new _PushpinPainter (glossy red pin) on 96px pink tile, circular pink bullet icons, full-width _EtaMapCard w/ in-card _CardLabel labels + _DestBubble (ramen icon), removed dead _RouteLabel. Root cause of "not matching" = WSL dev server (54180) serving STALE build. flutter analyze clean; web compiles+serves on 127.0.0.1:8090. Visual confirm pending. See sessions/2026-05-28-onboard-step4-eta-fidelity.md | lib/views/onboarding/onboarding_view.dart |
| 2026-05-30 07:53 | team-leader | plan onboarding 08/09 task; BLOCKER-002 no Task/subagent tool available — cannot dispatch coder/qa, escalated to user | plan.md, current-task.md, blockers.md |
| 2026-05-30 | team-leader (option-2 direct impl) | Implement onboarding 08/09 per plan: backend B1-B7 (003_onboarding.sql, models, auth handler/service, interfaces, user service, user handlers, main.go CORS+routes; gofmt'd) + Flutter F1-F6 (auth_service onboarding_done, api_client.patch, profile_service, astrology utils, Screen 08 user_profile_view, Screen 09 food_preferences_view, onboarding_view _routeAfterAuth routing). All IDE-flagged structural errors fixed (broken _navigateAway edit, Sparkle import). VERIFIED GREEN: `GO111MODULE=on GOPROXY=off go build ./...` rc=0 + `go vet ./...` rc=0 + `flutter analyze lib/` 0 errors/0 warnings (5 pre-existing info lints in untouched places_service.dart only). BLOCKER-003 resolved: root cause = shell GO111MODULE=off forcing GOPATH mode (not code); set GO111MODULE=on. Functional QA still pending. | anmates-api/db/migrations/003_onboarding.sql, anmates-api/{models/models,handlers/auth,services/auth,services/interfaces,services/user,handlers/user,main}.go, anmates_flutter/lib/services/{auth_service,api_client,profile_service}.dart, anmates_flutter/lib/utils/astrology.dart, anmates_flutter/lib/views/onboarding/{user_profile_view,food_preferences_view,onboarding_view}.dart, current-task.md, blockers.md, api-contracts.md |
| 2026-05-31 | main-assistant | Screen 08 UI polish (user-confirmed): horoscope_icons.dart (ZodiacIcon/NguHanhIcon/ThanSoIcon), DOB picker gradient (wisteria→berry) + magnifier, auto-detect label above icon + center-align, custom slider thumb (restaurant icon, r=12), zone labels with moon/sun icons + sub-text (Tám 1-1 / Cân bằng / Bàn 6+), TÍNH CÁCH BÀN ĂN restaurant icon, animated badge pill, slider card container. Nav bug fixed: pushReplacement→pushAndRemoveUntil in _routeAfterAuth. flutter analyze 0 errors. → R-003 | lib/widgets/horoscope_icons.dart (new), lib/views/onboarding/user_profile_view.dart, lib/views/onboarding/onboarding_view.dart |
| 2026-05-31 | main-assistant | refactor onboarding submit flow: Screens 08/09 → client draft (no API, validation: name/nickname letters-only + 16+, food 5–10, vibe 2–5, blue "Vibe buổi ăn" + 🚫Không hành), NEW Screen 10 photo upload (Firebase Storage, main required + 2 optional + captions, tips card, sources, match design), "Hoàn tất" validates all 3 + jumps back on error + one-shot PATCH /profile/complete-onboarding; NEW user_photos table; Screen 11 discover loads /profile (nickname+avatar). flutter analyze clean; Go build UNVERIFIED (no Go on Windows box). ⚠️ Firebase Storage Console enable+rules required | anmates-api/db/migrations/004_user_photos.sql (new), anmates-api/models/models.go, anmates-api/services/{user,interfaces}.go, anmates-api/handlers/{auth,user}.go, anmates-api/main.go, anmates_flutter/pubspec.yaml, anmates_flutter/lib/services/{onboarding_draft,storage_service,profile_service}.dart, anmates_flutter/lib/views/onboarding/{user_profile_view,food_preferences_view,photo_upload_view(new),onboarding_view}.dart, anmates_flutter/lib/views/discover/discover_view.dart, sessions/2026-05-31-onboarding-flow-refactor-screen08-09-10-11.md |
| 2026-06-01 | main-assistant | TROUBLESHOOT Screen 11 submit "Null check operator used on a null value": root cause = Storage bucket had NO CORS → web upload from 127.0.0.1:54180 blocked → null rejection trips null! in _flutterfire_internals _testException. Fixed: set CORS on gs://anmates-studio.firebasestorage.app. Verification pending user. | anmates_flutter/storage.cors.json (new), gs://anmates-studio.firebasestorage.app (CORS), sessions/2026-06-01-screen11-submit-null-check-cors.md |
| 2026-06-01 | main-assistant | RESOLVE Screen 11 onboarding submit — R-004 written (user confirmed). Root cause: firebase-storage.js missing from web/index.html pre-load block. Also fixed: food_tags validation 5-10→2-5, culture_tags full-stack (DB migration 005 + model + service + handler + Flutter), vibe/culture UI bounds (min1 max3 each), loading overlay. | web/index.html, storage.cors.json, anmates-api/db/migrations/005_culture_tags.sql, anmates-api/models/models.go, anmates-api/services/user.go, anmates-api/handlers/user.go, anmates_flutter/lib/services/profile_service.dart, anmates_flutter/lib/services/onboarding_draft.dart, anmates_flutter/lib/views/onboarding/{photo_upload_view,vibe_preferences_view,food_preferences_view}.dart, resolutions/R-004 |
| 2026-06-01 | main-assistant | CI deploy lên GitHub Environment `dev` (TECH-7): ci.flutter-web.yml + ci.go-api.yml thêm job `deploy-dev` (same-repo PR only) trỏ `environment: dev` — web→dev-anmates-studio.web.app, api→shared Cloud Run anmates-api (ENV=dev), build web bake API_BASE_URL chung. cd.* giữ nguyên (sẽ tách prod sau: Cloud Run riêng + Cloud SQL + Secret Manager). User chọn 1 env `dev` (không phải dev-web/dev-api). ⚠️ chưa tạo env `dev` + site dev-anmates-studio (máy không có gh). Docs updated. Verification pending. See sessions/2026-06-01-ci-dev-environment-deploy.md | .github/workflows/{ci.flutter-web,ci.go-api}.yml, .github/{CI-CD,WORKFLOW-ARCHITECTURE}.md, sessions/2026-06-01-ci-dev-environment-deploy.md |
| 2026-06-01 | main-assistant | Gộp env production: cd.flutter-web.yml + cd.go-api.yml đổi `production-web`/`production-api` → chung 1 env `production` (mirror cách dev gộp 1). Docs (CI-CD.md bảng env + Bước 4 tạo env, WORKFLOW-ARCHITECTURE.md § "One env per tier" + Phase 4) cập nhật. ⚠️ Cần tạo env `production` trên repo (gh chưa cài) + xoá production-web/api cũ. | .github/workflows/{cd.flutter-web,cd.go-api}.yml, .github/{CI-CD,WORKFLOW-ARCHITECTURE}.md |
| 2026-06-01 | main-assistant | User confirmed: tạo GitHub env `dev` + `production`, xoá `production-web`/`production-api` (qua UI, screenshot xác nhận). Env setup cho CI→dev + cd→production hoàn tất. Còn lại: Firebase site dev-anmates-studio + 1 PR thử để verify luồng deploy. | (GitHub repo Environments — no file change) |
| 2026-06-01 | main-assistant | User confirmed CI→dev deploy chạy OK. R-005 written: CI deploy GitHub Environment dev + gộp production env. GCS CORS apply (`gsutil cors set`) cho dev-anmates-studio.web.app. current-task → done. | resolutions/R-005-ci-dev-environment-deploy.md, resolutions/INDEX.md |
| 2026-06-04 | main-assistant | AI Concierge venue source: thay map/DB ingest bằng MCP web-search. NEW Python microservice `ai-venue-search/` (FastAPI, modular: SearchProvider[MCP/mock] + Structurer[Anthropic/mock], factory chọn theo env, fallback mock khi thiếu MCP cmd/API key) → POST /suggest trả picks. Go: thêm `services.VenueProvider` interface; `WebSearchProvider` (HTTP client gọi service Python, tự recompute distance từ midpoint, bỏ anti-hallucination — tin web search) + `DBLLMVenueProvider` (legacy restaurants table + LLM rank, fallback) implement nó; `ConciergeService` dùng provider thay cho venues+llm trực tiếp; config `AI_SEARCH_URL` (ưu tiên) / `AI_BASE_URL` (fallback); main.go wiring chọn mode. Go test search_client_test.go (httptest) + Python tests/test_mock_smoke.py. ⚠️ Build UNVERIFIED (Go+Python không có trên PATH host — verify qua Docker/CI). See sessions/2026-06-04-ai-concierge-mcp-websearch.md | ai-venue-search/** (new), anmates-api/services/{concierge,venue_provider(new),search_client(new),search_client_test(new)}.go, anmates-api/{config/config,main}.go, anmates_flutter/lib/widgets/ai_venue_card.dart |
| 2026-06-04 | main-assistant | ai-venue-search free/keyless defaults (user: "free hết, không api key"): web search default = DuckDuckGo MCP (`uvx duckduckgo-mcp-server`, free, tool=search); structurer default = local LM Studio/Ollama (OpenAICompatStructurer, LLM_BASE_URL=host.docker.internal:1234/v1, no key, reasoning_content fallback). Anthropic demoted to optional. `uv` added to requirements; Dockerfile drops Node. .env.example/README updated. Mock fallbacks kept. | ai-venue-search/{requirements.txt,Dockerfile,.env.example,README.md,ai_venue_search/config.py,ai_venue_search/providers/structurer.py} |
| 2026-06-04 | main-assistant | Apply github.com/zebbern/no-cost-ai vào ai-venue-search: thêm structurer "pollinations" = Pollinations free OpenAI endpoint (https://text.pollinations.ai/openai, ẩn danh ~1 req/15s, KHÔNG key, KHÔNG cần model local) → đặt làm DEFAULT structurer. OpenAICompatStructurer refactor nhận chat_url/model/key tường minh (dùng chung cho pollinations preset + LM Studio preset). Config thêm POLLINATIONS_MODEL/TOKEN; STRUCTURER default openai→pollinations. .env.example/README cập nhật: zero setup (chỉ cần `uv` cho DDG MCP). LM Studio (openai) + Anthropic vẫn là tuỳ chọn. | ai-venue-search/{ai_venue_search/config.py,ai_venue_search/providers/structurer.py,.env.example,README.md} |
| 2026-06-04 | main-assistant | Wire ai-venue-search vào docker-compose + VERIFY LIVE (Docker). Compose: service `ai_venue_search` (build ./ai-venue-search, port 8090, healthcheck, extra_hosts host.docker.internal), api được set AI_SEARCH_URL=http://ai_venue_search:8090 + depends_on healthy. Dockerfile pre-install `uv tool install duckduckgo-mcp-server`. Robustness thêm: (1) reverse-geocode midpoint qua Nominatim/OSM (free,no-key) → query theo địa danh thay lat/lng thô (geocode.py); (2) FallbackStructurer chuỗi pollinations→LM Studio (Pollinations anon tier 429 liên tục → tự rớt sang LM Studio); (3) json_schema strict cho path LM Studio (json_object bị 400; schema giống llm.go). VERIFIED: POST /suggest 200, DDG search 200, geocode "Phường Sài Gòn", qwen3.5-9b trả picks tên quán cụ thể (Lẩu Dê 6 Tửng/Ashima), tiếng Việt sạch, provider=mcp+pollinations+openai. ⚠️ Hạn chế: web search trả listicle → lat/lng thường 0 (pin map kém), tên quán không đảm bảo grounded 100%. .env/.env.example set AI_SEARCH_URL. | docker-compose.yml, .env, .env.example, ai-venue-search/{Dockerfile,ai_venue_search/geocode.py(new),ai_venue_search/service.py,ai_venue_search/providers/structurer.py} |
| 2026-06-04 | main-assistant | Xử lý hạn chế web-search + VERIFY LIVE (chuẩn bị Codex review). ai-venue-search thêm grounding: (1) MCP fetch_content cào top-N trang kết quả → model bóc tên+địa chỉ quán THẬT thay vì tiêu đề listicle (config MCP_FETCH_TOP_N/CHAR_CAP); (2) geocode.py đổi Nominatim→**Photon** (komoot, OSM, mạnh POI/địa chỉ VN hơn) cho cả reverse(midpoint→khu vực) + forward(quán→lat/lng thật); forward bias theo midpoint + reject kết quả type thô (city/locality) + guard haversine >60km (chặn pin sai thành phố); service._clean_address bỏ "Tầng.../Lầu...", lấy số nhà+đường; luôn geocode (không tin toạ độ model echo midpoint). Prompt: name phải là tên quán cụ thể + lat/lng luôn 0. Timeouts nới (sidecar 55s, Go client 70s, concierge ctx 75s). VERIFIED qua Docker: picks ra Haidilao/Kichi/Lẩu Lil'Sago với toạ độ HCMC phân biệt thật, địa chỉ thật; quán không định vị được → lat/lng=0 (không ghim sai). Offline smoke test PASS trong image. Hạn chế còn lại (chấp nhận): 1 số quán lat/lng=0, rating/price hay null, tên không grounded 100%. | ai-venue-search/{ai_venue_search/{config,service,geocode,providers/search,providers/structurer}.py,tests/test_mock_smoke.py,README.md,.env.example}, docker-compose.yml, anmates-api/services/{search_client,concierge}.go |
| 2026-06-06 | main-assistant | AI Concierge **pre-warm cache** + **2-user side-by-side E2E**. (1) Latency fix: card was slow (web-search 30–60s). Added `AI_WARM_POINTS` (default trigger−10=60) → concierge prefetches `provider.Suggest()` in background when Vibe enters `[warm,trigger)`, parks result in in-memory cache (sync.Mutex + warm/warming maps, 10min TTL); `fire` consumes cache (takeWarm) for instant post, else falls back to fresh `compute`. Refactor: pure `decideAction(before,after,warm,trigger)→none/warm/fire`, extracted `compute` (members→locations→mood→Suggest, status ok/error/skipped), `prewarm`, `alreadyFired`. (2) New 2-phone video script: `.dev-e2e/docker-compose.e2e.yml` adds 2nd host port 54181→flutter_web (distinct origin so An/Bình don't share localStorage); `e2e_two_users.js` dev-logins both, seeds match@58, drives 12-line scripted convo over each user's WS → Vibe 58→70 (prewarm@60, fire@70) → card on BOTH phones → reload for full-history money shot; `run-e2e.ps1` one-command runner (STRUCTURER=openai/LM Studio). VERIFIED GREEN in Docker golang:1.25-alpine: `go build ./...`+`go vet` clean, services tests 10/10 incl new TestDecideAction (smoke pkg fails = needs live :8080, unrelated). **VIDEO RECORDED** ✅: drove the real stack (LM Studio qwen3.5-9b) → `videos/ai-concierge-2users.webm` (~1.3MB, 97s) An+Bình side-by-side, card on both phones (Cơm Niêu Sài Gòn/Nhà Hàng Khoái/Lobster Bay). GOTCHA: Flutter CanvasKit renders BLANK in iframe → pivoted to 2 separate pages + dockerized ffmpeg hstack. FOLLOW-UP: user reported "peer messages don't show" → diagnosed as TEST ARTIFACT not app bug (repro: Bình's phone DOES render An's live WS msg; hub broadcast excludes only sender by user-id; card reaching both already proved receive path). Each phone looked one-sided because script injects via raw WS (not the canvas composer) so the sending phone never optimistically appends. Fixed VIDEO (no prod code change): pre-seed all-but-last-2 lines as real `messages` history (both sides, staggered created_at) + points=70−LIVE, send only short live tail to fire card → both phones load full two-sided chat on open + card live. Re-recorded. See sessions/2026-06-06-ai-concierge-prewarm-2user-e2e.md | anmates-api/{config/config,main,services/concierge,services/concierge_test}.go, .dev-e2e/{e2e_two_users.js,docker-compose.e2e.yml,run-e2e.ps1}, videos/ai-concierge-2users.webm |
| 2026-06-05 | main-assistant | LIVE E2E + VIDEO of AI Concierge (web-search). Drove the real flow on Docker: dev-login 2 users (no Firebase, DEV_MODE), seeded match+noi_lau(69)+user_locations, added TEMP dev deep-link in main.dart (?dev_match&dev_phone→devLogin→live ChatDetailView), Playwright (headed) recorded user A's Flutter chat while user B sent a WS message crossing Vibe→70. Real pipeline (DuckDuckGo MCP + LM Studio qwen3.5-9b) returned real VN venues → card broadcast over WS → rendered in UI. Evidence: .dev-e2e/ai-concierge-live-test.webm + ai-card-screenshot.png (Moo Beef Steak, Sài Gòn Xưa Và Nay). BUGFIX (kept): search_client.go coerces mood nil→[] (sidecar mood_tags List[str] rejects null → was 422 for tagless users). NOTED: DB drift (no `level` col → /progress 500, paywall off); main.dart deep-link must be reverted. See sessions/2026-06-05-ai-concierge-live-e2e-video.md | anmates-api/services/search_client.go, anmates_flutter/lib/main.dart (TEMP), .dev-e2e/** |
| 2026-06-06 | main-assistant | AI venue card: tap venue name → Google Maps deep-link (web new tab / mobile app else web); decided MVP drops open_now/business_status; refused stealth Maps scraper (ToS/evasion) | anmates_flutter/lib/services/maps_launcher.dart, anmates_flutter/lib/widgets/ai_venue_card.dart, sessions/2026-06-06-ai-venue-card-maps-deeplink.md |
| 2026-06-06 | main-assistant | free-path: thread venue `address` sidecar→Go cardPick→Flutter model so Maps deep-link is accurate when web-search lat/lng=0; deferred place_id/review_count (need keyed API) | anmates-api/services/concierge.go, anmates-api/services/search_client.go, anmates_flutter/lib/models/ai_venue_card.dart, anmates_flutter/lib/widgets/ai_venue_card.dart |
| 2026-06-06 | main-assistant | concierge "too far apart" guard: skip + post friendly notice when 2 users > AI_MAX_SEPARATION_M (default 50km); migration 009 allows status skipped_too_far; travel-time midpoint deferred (needs keyed routing API) | anmates-api/services/concierge.go, concierge_test.go, config/config.go, main.go, db/migrations/009_concierge_too_far.sql, .env.example |
| 2026-06-06 | main-assistant | concierge anchor selection: POST /matches/:id/concierge/suggest {anchor: midpoint|me|mate} (private re-roll, no chat post) + SuggestForUser/resolveAnchor + Flutter anchor chips on venue card (Điểm giữa/Gần mình/Gần mate) | anmates-api/services/concierge.go, concierge_test.go, handlers/concierge.go, main.go, anmates_flutter/lib/services/concierge_service.dart, lib/widgets/ai_venue_card.dart, lib/views/chat/chat_detail_view.dart, test/ai_venue_card_test.dart |
| 2026-06-06 | main-assistant | VERIFIED 06-06 chain: Go build/vet/test GREEN (fixed TestTooFarNotice emoji→CJK; +TestResolveAnchor), Flutter ai_venue_card_test 5/5 (fixed stale priceLabel assert), live 2-user video recorded (.dev-e2e/videos/ai-concierge-2users.webm) w/ anchor chips + Maps deep-link visible; widget_test.dart pre-existing-broken (TEMP main.dart) | anmates-api/services/concierge.go, anmates_flutter/test/ai_venue_card_test.dart, .dev-e2e/videos/ |
| 2026-06-06 | main-assistant | anchor-chip TAP demo video recorded (.dev-e2e/e2e_anchor_demo.js → ai-concierge-anchor-demo.webm): live taps Gần mình→775m / Gần An→Moo Beef Steak Q1 / Điểm giữa→1.6km, all 200; documented CanvasKit tap-by-coord + waitForRequest/Response gotchas | .dev-e2e/e2e_anchor_demo.js, .dev-e2e/videos/ |
| 2026-06-06 | main-assistant | sidecar page-fetch filtering: skip blocked domains (fb/shopee/maps…), 403/captcha/error pages, and non-food pages (≥3 food-term gate); larger candidate pool to backfill skips; +tests 4/4 (incl. _fetch_pages integration) | ai-venue-search/ai_venue_search/providers/search.py, ai-venue-search/tests/test_page_filters.py |
| 2026-06-06 | main-assistant | Make swipe→match→chat flow REAL + nav rewire. Bottom tab 4 "Mình"→"Ăn Match" (opens SwipeView); profile now via avatar tap on Khám phá (+back btn). SwipeView rewritten mock→real: loads MatchService.getCandidates()+currentUserId, renders real candidate (name/avatar/overlap-food chips/hợp-gu%), swipe-right/♥→acceptMatch→MatchView→"Hello"→live ChatDetailView(matchId); pass→next; loading/error/empty states. BLOCKER fixed: matching used `wishlists` (never written by app) → deck always empty; switched ListCandidates+AcceptMatch score to `users.food_tags` Jaccard (onboarding-populated), threshold ≥1 shared tag → 2 real users matchable right after onboarding. Accept stays one-sided+instant. VERIFIED Docker golang:1.25: go build rc=0, go vet rc=0, go test ./services ok (smoke fails=needs live :8080). Flutter NOT analyzed (no host PATH) — verify via start.sh. See sessions/2026-06-06-real-swipe-match-chat-flow.md | anmates-api/services/matching.go, anmates_flutter/lib/views/{main_tab_view,match/swipe_view,discover/discover_view,profile/profile_view}.dart, anmates_flutter/lib/widgets/anm_widgets.dart |
| 2026-06-06 | main-assistant | Resolve follow-ups: mutual-like + wishlist CRUD + threshold≥2. (1) Mutual-like gate replaces one-sided instant match: new migration 010_swipes.sql (swipes table); matching.go AcceptMatch→Swipe(like/pass, reciprocity→createMatch)+Undo(rewind), ListCandidates excludes swiped; interfaces/handlers/main routes /matches/:id/accept→POST /swipes + /swipes/undo. (2) Wishlist real: new lib/services/wishlist_service.dart + WishlistView rewritten mock→CRUD (list/add-sheet/delete, optimistic, 409 handling, states). (3) Matching interest set = food_tags ∪ wishlist food_name ∪ wishlist food_category (3 disjoint vocabularies, no collision), threshold raised back to ≥2. Flutter: match_service acceptMatch→swipe()+SwipeResult+undoSwipe; swipe_view mutual-like UX + rewind button restored. smoke_test updated to /swipes flow. VERIFIED: Docker go build/vet rc=0 (incl smoke), go test ./services ok; **SQL validated LIVE on Postgres 16** (.dev-e2e/matching_sql_check.sql: Bob food_tag-match .5 + Eve wishlist-category-match .333, Carol<2 & Dave!onboarded excluded, swipe-exclusion works). Flutter NOT analyzed (no host PATH). See sessions/2026-06-06-real-swipe-match-chat-flow.md | anmates-api/{db/migrations/010_swipes.sql,services/matching.go,services/interfaces.go,handlers/matching.go,main.go,smoke/smoke_test.go}, anmates_flutter/lib/{services/match_service.dart,services/wishlist_service.dart,views/match/swipe_view.dart,views/discover/wishlist_view.dart}, .dev-e2e/matching_sql_check.sql |
| 2026-06-06 | main-assistant | Pre-deploy CI gate + commit. Ran the GitHub CI gates locally via Docker before committing the swipe→match→chat / mutual-like / wishlist-CRUD work: golangci-lint v2.12.2 → 0 issues; go build ./...; go test -race (excl smoke) ok; flutter analyze --no-fatal-infos (6 pre-existing infos, none in changeset); flutter test 6/6; dart format applied to swipe_view.dart + wishlist_view.dart. Confirmed main.dart TEMP deep-link already reverted (not in changeset). Migrations auto-apply on API startup (db/migrate.go) so 010_swipes.sql needs no manual DB step on dev. Committed 858e68a (17 files). NOT pushed yet — dev deploy triggers on same-repo PR to main (R-005). Not run locally: flutter build web + Dockerfile docker-build gate (run in GH CI, low-risk: compile+lint green). | (commit 858e68a; changelog) |
| 2026-06-07 | main-assistant | fix session persistence: splash restores session (was always→onboarding) + ApiClient silent refresh-on-401 + AuthService.refreshSession/clearSession; keeps user logged in ~7d via refresh token | anmates_flutter/lib/views/splash/splash_screen.dart, lib/services/api_client.dart, lib/services/auth_service.dart, sessions/2026-06-07-session-persistence-keep-logged-in.md |
| 2026-06-07 | main-assistant | add automation tests for session persistence (5 tests: refreshSession + ApiClient refresh-on-401) + testability refactor (injectable http.Client); fix smoke test for new splash startup; verified 11/11 green on host. CI runs them via existing flutter test step | anmates_flutter/test/session_persistence_test.dart, test/widget_test.dart, lib/services/api_client.dart, lib/services/auth_service.dart, lib/views/splash/splash_screen.dart |
| 2026-06-07 | main-assistant | fix prod CD gaps for AI Concierge before main-merge. (1) cd.go-api.yml prod deploy was MISSING AI_SEARCH_URL (only dev/ci.go-api set it); Cloud Run --set-env-vars REPLACES env set + Go has no default → prod deploy would silently DISABLE AI Concierge. Added AI_SEARCH_URL (vars||fallback URL) to match dev. (2) ai-venue-search had only ci (dev deploy on PR), NO cd → sidecar never reached prod on merge. Created cd.ai-venue-search.yml (push main + ai-venue-search/**, pytest→build/push :sha+:latest→prev-revision capture→Cloud Run 8090/1Gi/120s/STRUCTURER=pollinations→/health smoke→rollback), modeled on cd.go-api.yml. NOT yet run in GH Actions (no host YAML linter). ⚠️ verify repo var AI_SEARCH_URL. See sessions/2026-06-07-prod-cd-gaps-ai-search-url-sidecar.md | .github/workflows/cd.go-api.yml, .github/workflows/cd.ai-venue-search.yml |
| 2026-06-07 | main-assistant | E2E full-flow test (dev-login→onboarding→match→WS chat→AI card) via new `.dev-e2e/e2e_full_flow.js` (Node fetch+WebSocket). 16/16 main-flow steps PASS incl. AI Concierge `ai_venue_card` fired end-to-end (real web-search venues). Found+fixed: (A) stale api image missing migration 010_swipes → rebuilt; (B) local pgdata volume missing `noi_lau_progress.level` (phantom migration 003_noi_lau_drop_level no longer in repo) → match-create 500 → ALTER ADD COLUMN; (C) design conflict paywall lv3(30pts) blocks Concierge trigger(70pts) → BLOCKER-004 (needs product decision). No production source changed. | .dev-e2e/e2e_full_flow.js (new), live: rebuilt anmates-api image + ALTER noi_lau_progress; sessions/2026-06-07-e2e-full-flow-test-2users-chat.md (new), current-task.md, blockers.md (BLOCKER-004) |
| 2026-06-08 | main-assistant | Remove chat paywall for MVP market test (BLOCKER-004 fix): `services/chat.go CheckPaywall` now always returns false (drop level-3 hard-lock that capped chat at 30 pts and blocked the 70-pt AI Concierge trigger) — per LOCKED Phase-1 "MVP FREE". Comment marks where Phase-2 quota gate goes. Rebuilt api image. E2E `.dev-e2e/e2e_full_flow.js` now 20/20 GREEN: Vibe climbs 0→72 naturally, AI `ai_venue_card` fires end-to-end (King BBQ·Subin BBQ). Added 25s warm-settle pause in e2e to avoid sidecar 502 from concurrent warm(60)+fire(70) LLM calls under burst. | anmates-api/services/chat.go, .dev-e2e/e2e_full_flow.js, sessions/2026-06-07-e2e-full-flow-test-2users-chat.md, current-task.md, blockers.md |
| 2026-06-08 | main-assistant | Record 2-user AI Concierge video (`.dev-e2e/e2e_two_users.js`): side-by-side An↔Bình, 12 live-typed msgs cross Vibe 70 → `ai_venue_card` fires on BOTH phones (King BBQ·Sumo BBQ·Subin BBQ), UI render confirmed via screenshot. First attempt failed `skipped_preconds` (seeded users had no food_tags → provider 0 picks) → onboarded both video users with food_tags. Also switched sidecar structurer LLM_MODEL qwen3.5-9b→qwen/qwen2.5-vl-7b (9b reasoning model ~16s even trivial → sync suggest 60s ReadTimeout; vl-7b ~9s, suggest 200 in 20.5s). Minor UI nit: header Vibe counter loads from /progress once, doesn't increment live per WS msg (still shows 58 when card fires) — card render itself works. Output: videos/ai-concierge-2users.webm + 02_A/B_card.png | .dev-e2e/e2e_two_users.js (run), live: onboarded +84900000001/2, sidecar→vl-7b; videos/ |
| 2026-06-08 | main-assistant | FIX live Vibe counter (user report: số 58/100 đứng yên, phải leo theo tin nhắn). `chat_detail_view.dart`: `_bumpVibe()` (+1 clamp 0-100) called on `_sendMessage` (own sends don't echo back) + `_onIncoming` for text/image msgs (skip ai_venue_card/system — backend doesn't score those); mirrors backend +1/msg, authoritative value still reloaded from /progress on open. Verified via re-recorded 2-phone video: header climbed 58→70 live, hit UNLOCKED + "Chốt First Date mở rồi", AI card fired (Buffet·King BBQ·Sumo BBQ). NOTE: had to re-add TEMP deep-link handler in main.dart (_resolveHome + _DevChatDeepLink reading dev_match/dev_phone/dev_mate) — it had been reverted from repo, and rebuilding flutter_web dropped it; REVERT main.dart deep-link before commit. | anmates_flutter/lib/views/chat/chat_detail_view.dart, anmates_flutter/lib/main.dart (TEMP deep-link), videos/ai-concierge-2users.webm |
| 2026-06-08 | main-assistant | RE-RUN E2E full-flow (node .dev-e2e/e2e_full_flow.js) on live stack -> **20/20 PASS** (dev-login->onboarding->prefs->location->deck overlap=6->mutual-like->match->conversations->WS 2-way->Vibe 0->72->AI ai_venue_card King BBQ.Subin BBQ). Catalogued 8 issues from inspecting the real card + container state: ISSUE-1 web-search venue grounding (restaurant_id empty + Subin BBQ coords 184m from midpoint vs Thu Duc address ~13km; anti-hallucination only on DB path); ISSUE-2 wrong area name in intro ("Xuan Hoa"/Vinh Phuc for HCMC midpoint); ISSUE-3 only 2 picks (cap 3, no top-up); ISSUE-4 sidecar 502 on warm+fire concurrency (E2E masks w/ 25s pause); ISSUE-5 API container perpetually unhealthy (healthcheck /health route missing, FailingStreak 301); ISSUE-6 dead paywall error branch; ISSUE-7 TEMP dev deep-link still in main.dart (revert pre-merge); ISSUE-8 client vibe mirror drift. Catalog only, no prod source changed. | sessions/2026-06-08-e2e-full-flow-issues.md (new) |
| 2026-06-08 | main-assistant | FIX nhom A+B tu E2E issue catalog (user chon "1"). **A2** (intro sai ten khu vuc "Xuan Hoa"): bo prepend area vao context model (OSM+Nominatim deu tra district sai cho midpoint trung tam HCM) + prompt cam intro neu ten phuong/quan (service.py, structurer.py). **A1** (toa do venue bia/mau thuan dia chi): radius guard dong _accept_radius_km=max(6km,radius*1.5) thay _MAX_GEO_KM=60; co street address thi CHI tin street, hit ngoai ban kinh -> drop lat/lng=0 (khong fallback fuzzy name -> het loi Subin BBQ Thu Duc ghim 184m) (service.py). **B2** (api container unhealthy FailingStreak 301): healthcheck localhost->127.0.0.1 (busybox wget resolve localhost=IPv6 ::1, Go listen IPv4 -> connection refused) (docker-compose.yml). New tests/test_geocode_radius.py (6 case). Verify: pytest tests/=11 passed, rebuild sidecar + recreate api=healthy, E2E full-flow 20/20 PASS, card that: King BBQ 1.10km/PandaBBQ 1.96km/Mini Candy 2.85km in-area dist khop, intro "giua 2 ban" het Xuan Hoa. Khong dung Go/Dart. Chua user-confirm UI -> R-006 khi confirm. | docker-compose.yml, ai-venue-search/ai_venue_search/{service,providers/structurer}.py, ai-venue-search/tests/test_geocode_radius.py (new), sessions/2026-06-08-e2e-full-flow-issues.md |
| 2026-06-08 | main-assistant | LIVE test "dia chi hien tai" cua user (VPN off) + VIDEO. IP-geo -> HCMC ~10.82,106.63 (Go Vap/Tan Phu, FPT). Parametrize e2e_two_users.js seed location qua env (LAT_A/LNG_A/LAT_B/LNG_B, default=vi tri user). Probe /suggest: The Chill BBQ Tan Phu 1.45km grounded. Video 2-phone (Playwright+ffmpeg): chat live -> Vibe 70 -> card fire ca 2 phone. A1 verified thuc dia: BBQ Garden (Go Vap) ghim dung 3.5km; Motsunabe (Q1 ~7km) + Pachi Pachi (mo ho) -> lat/lng=0 KHONG pin gia. A2: intro khong ten khu vuc sai. New ISSUE-9 (UI nit): card hien "0m" cho quan lat/lng=0 -> an distance khi 0,0 (nhom C). Output videos/ai-concierge-2users.webm + 02_{A,B}_card.png. | .dev-e2e/e2e_two_users.js, videos/ai-concierge-2users.webm, sessions/2026-06-08-e2e-full-flow-issues.md |
| 2026-06-08 | main-assistant | FIX vi tri sai (user o Thu Duc nhung IP-geo tra Go Vap ~13km lech). Root cause: IP geolocation tra vi tri ISP (FPT) khong phai vi tri thuc -> app prod dung GPS PUT /me/location nen khong bi; IP chi la shortcut test. Geocode "Duong so 2, Phuong Thu Duc" (Nominatim) = 10.8383,106.7497. Re-run: ca 3 quan deu o Thu Duc (King BBQ Buffet 216 Vo Van Ngan, Sumo Yakiniku Kha Van Can, Buffet Song Sac 4.0km), intro "khu vuc Thu Duc". Video lan 1 thieu card do B1 (prewarm+fire goi LM Studio dong thoi -> skipped_preconds); them settle-pause 25s sau warm-point vao e2e_two_users.js (mirror e2e_full_flow) -> card fire OK. Video + 02_{A,B}_card.png updated. e2e_two_users.js parametrize location qua env. | .dev-e2e/e2e_two_users.js, videos/ai-concierge-2users.webm |
| 2026-06-08 | main-assistant | "Lam tat ca": FIX ISSUE-9 + B1 + D1 test + R-006. **ISSUE-9**: ai_venue_card.dart distanceLabel tra rong khi lat==0&&lng==0 (het "0m" cho quan chua dinh vi) + test. **B1**: ConciergeService.fire cho prewarm in-flight xong (channel trong warming map) roi takeWarm thay vi goi LLM lan 2 dong thoi -> het 502; verify SETTLE_MS=0 (e2e_full_flow KHONG pause) card van fire 20/20; go vet+test ok. **D1**: them .dev-e2e/e2e_card_buttons.js (Playwright tap "Goi y cho Mate" -> assert proposal msg gui qua WS) PASS. **R-006**: migrate A1/A2/B2 sang resolution (user confirmed qua live Thu Duc test + video). Rebuild flutter_web xac nhan ISSUE-9 truc quan. Verify: pytest 11, go test ok, e2e 20/20, D1 PASS, card render dung. | anmates_flutter/lib/models/ai_venue_card.dart, anmates_flutter/test/ai_venue_card_test.dart, anmates-api/services/concierge.go, .dev-e2e/{e2e_full_flow.js,e2e_card_buttons.js(new)}, resolutions/R-006-ai-concierge-grounding-healthcheck.md (new), resolutions/INDEX.md |
| 2026-06-08 | main-assistant | "Lam het" phan con lai (bounded): **C1** handlers/chat.go bo message paywall chet "chat locked at level 3"->"chat temporarily locked" + comment seam Phase-2 (CheckPaywall luon false). **C2** main.dart: gate dev deep-link sau _devDeepLinkEnabled (kDebugMode || apiBaseUrl localhost/127.0.0.1) -> INERT o prod web build (real API domain), van chay dev/e2e; bo nhan "revert before shipping" (gio prod-safe). **D** e2e_full_flow.js +step 12 coverage: profile GET/PUT, wishlist CRUD (POST/GET/DELETE), swipe+undo (tren candidate phu, khong pha match A-B). Verify: api+flutter_web build OK (Go+Dart compile), go vet+test services ok, e2e 26/26 PASS (was 20). A1-restaurant_id + nhom E: defer (xem ghi chu). | anmates-api/handlers/chat.go, anmates_flutter/lib/main.dart, .dev-e2e/e2e_full_flow.js |
| 2026-06-08 | main-assistant | FEATURE First Date / Booking (nhom E, full-stack). Backend: migration 011_bookings (1 active booking/match via partial unique index), models.Booking, services/booking.go (Propose replace-active / Get / Confirm[chi non-proposer] / Cancel + IsMember + validateProposal pure), BookingServicer interface, handlers/booking.go (4 routes + error mapping), main.go wiring. Routes POST/GET /matches/:id/booking + /confirm + /cancel. Flutter: booking_service.dart (Booking model + propose/current/confirm/cancel), booking_view.dart rewrite mock->real (calendar dong theo thang hien tai, load existing booking, banner trang thai propose/confirm/cancel, CTA propose that), wire chat CTA "Chot First Date" -> BookingView voi venue tu AI card gan nhat. Verify: go build+vet+test (TestValidateProposal) ok, e2e_full_flow 31/31 (them step 13 propose/confirm-own-409/confirm/cancel), flutter build OK, booking UI smoke PASS (tap CTA->BookingView->propose->DB row). Defer: chat broadcast on confirm, check-in/voucher (can Live Tracking). | anmates-api/db/migrations/011_bookings.sql, anmates-api/models/models.go, anmates-api/services/{booking,booking_test,interfaces}.go, anmates-api/handlers/booking.go, anmates-api/main.go, anmates_flutter/lib/services/booking_service.dart, anmates_flutter/lib/views/booking/booking_view.dart, anmates_flutter/lib/views/chat/chat_detail_view.dart, .dev-e2e/{e2e_full_flow.js,e2e_booking_ui.js(new)}, api-contracts.md |
| 2026-06-08 | main-assistant | Full rebuild stack (docker compose up -d --build) cho test 1 lan; fix flutter_web HEALTHCHECK localhost->127.0.0.1 (cung loi IPv6 nhu B2, ap dung lan build sau); them .dev-e2e/seed_demo.js (dev-login 2 user + onboard + prefs + location Thu Duc + match @ vibe 68) -> deep-link de test AI card + booking nhanh. All containers healthy, AI Concierge enabled trigger 70. | docker-compose.yml(api healthcheck truoc do), anmates_flutter/Dockerfile, .dev-e2e/seed_demo.js (new) |
| 2026-06-08 | main-assistant (QA) | QA pass + VIDEO tren UI that (.dev-e2e/qa_full_flow.js, Playwright): chat -> Vibe 70 -> AI card (King BBQ Buffet Thu Duc, intro khong ten sai, khong "0m") -> Chot First Date -> BookingView (venue tu card) -> propose -> banner DA DE XUAT. 5/5 PASS, API-asserted moi buoc. Verify visual A1/A2/ISSUE-9 + booking. Video videos/qa-first-date-flow.webm + 4 screenshots. QA report qa-reports/2026-06-08-ai-concierge-booking.md. | .dev-e2e/qa_full_flow.js (new), videos/qa-first-date-flow.webm, qa-reports/2026-06-08-ai-concierge-booking.md (new) |
| 2026-06-08 | main-assistant (QA) | full-feature bug sweep (live API probe all routes + code review). Found CRITICAL auth-bypass: `httputil.Err` returns nil (Fiber c.JSON→nil) → `JWT`/`WSAuth` middleware fall through to c.Next() with uuid.Nil (BLOCKER-005). +3 minor: PUT /me/location stores Null Island (0,0) on missing lat/lng (non-pointer float64), web container stale-image healthcheck uses `localhost` (IPv6→unhealthy; Dockerfile already fixed, needs rebuild), malformed compose default `API_BASE_URL=http://127.0.0.1/:8080`. Verified OK: booking authz/confirm-own-409/outsider-404, wishlist category Flutter⇄Go match, swipe/undo, location range check. | qa-reports/2026-06-08-full-feature-explore-bugs.md, blockers.md (BLOCKER-005) |
| 2026-06-08 08:24 | coder | Fix QA bugs (BLOCKER-005 resolved): JWT auth gate (ValidateBearer returns errNoAuth, JWT/WSAuth render 401), location *float64 nil-check, docker-compose API_BASE_URL typo, rebuild web image→healthy; auth_test.go 3/3 PASS, go build rc=0 | anmates-api/middleware/auth.go, anmates-api/middleware/auth_test.go, anmates-api/handlers/chat.go, anmates-api/handlers/location.go, docker-compose.yml |
| 2026-06-08 | main-assistant (QA) | regression round after BUG-1..4 fixes — ALL GREEN, no regressions. Stack 4/4 healthy (web now healthy). Negative-auth matrix 27/27 (all 20 protected routes → clean 401 + error body, no-token & garbage-token). Booking/location/swipe matrix 17/17 (location {} /partial → 400 "lat/lng required", explicit 0,0 allowed; booking propose-missing/past→400, confirm-own→409, partner→200, outsider→404). Go unit tests middleware(+auth_test) ok, services ok. Go smoke suite live (SMOKE_BASE_URL host) PASS incl unauth assertions. e2e_full_flow.js 31/31 (WS chat Vibe 0→72, AI Concierge card 3 venues King/Sumo/Subin BBQ, booking propose/confirm/cancel). | (verification only — no source changed) |
| 2026-06-08 | coder | De-mock DiscoverView: real location via LocationService.currentLatLng (new method, fallback Quan 1), Overpass OSM nearby venues sorted by distance, client-side search TextField (debounced 250ms), genre card onTap filter (Lau/Nuong/Cafe/An vat), real _RestaurantRow (name/tag/distance from OsmPlace), openingHours chip replaces fake "nguoi dang them", nearest row gets fire emoji, MapsLauncher.open on tap, async Nominatim location label with fallback, loading/empty/error states; flutter analyze 0 errors, flutter build web --release succeeded | anmates_flutter/lib/services/location_service.dart, anmates_flutter/lib/views/discover/discover_view.dart |
| 2026-06-08 | coder | Discovery web-search: PART A sidecar (query field + query-led build_query + /search endpoint + test_build_query.py), PART B Go (searchReq.Query, mapPicks helper, WebSearchProvider.SearchText, handlers/venue.go GET /api/v1/venues/search auth+cache, main.go route, search_client_test.go /search case), PART C Flutter (venue_search_service.dart VenueResult+search, discover_view.dart submit→_submitSearch+_VenueResultRow+distance guard). Build: go build rc=0 (Docker), flutter build web rc=0 (Docker). Live E2E: q=bún bò giáo toàn → {"name":"Bún Bò Giáo Toàn","address":"218 Quốc lộ 1K, P.Linh Xuân, TP.Thủ Đức",...}; q=lẩu dê → 6 venues with coords+distances; no-token → 401 clean. | ai-venue-search/ai_venue_search/schemas.py, service.py, providers/structurer.py, app.py, tests/test_build_query.py, anmates-api/services/search_client.go, services/search_client_test.go, handlers/venue.go, main.go, anmates_flutter/lib/services/venue_search_service.dart, lib/views/discover/discover_view.dart |
| 2026-06-08 | main-assistant (QA) | verify Discovery de-mock (real OSM data) — PASS. Confirmed mock removed (0 "người đang thèm"/hardcoded rows), PlacesService.getNearby + TextField search + genre onTap filter + MapsLauncher tap + loading/error/_places states all wired; LocationService.currentLatLng added. Live data-source check: Overpass query (same as places_service) → 60 real Quận 1 venues (Phở Số 1 Hà Nội, Cà Phê Acoustic/Sỏi Đá, Lotteria, KFC…); Nominatim reverse OK (label "Phường Sài Gòn"; note R-006 — city mislabel handled by fallback). flutter analyze 0 errors, build web OK. Rebuilt flutter_web container → healthy, serves 200, new bundle contains overpass wiring. Discovery now finds real quán. | discover_view.dart, location_service.dart (coder) + changelog |
| 2026-06-08 | main-assistant (QA) | verify web-search venue search (Discovery) — MECHANICS PASS, relevance is web-search-variant. Endpoint GET /venues/search: no-token→401, q<2→400, 200+picks, distance guard OK (Kissho 0,0 → no dist). Flutter bundle deployed (venues/search present), api-contracts updated, 3 containers healthy. Relevance finding: dish/keyword search EXCELLENT ('lẩu dê'→6 real lẩu dê quán: Cầu Xéo/Tri Kỷ/Lâm Ký/Trương Định/Bảy Hồng/306; 'bún bò'→Bún Bò Hẻm). EXACT named-venue search UNRELIABLE: 'bún bò giáo toàn' across runs returned the exact quán once (coder run), else other bún bò/unrelated Q1 venues (B3 Steakhouse/Poke/Gogi) — inherent web-search+structurer non-determinism (expected trade-off, documented). Cache (10min, lat/lng rounded 3dp) confirmed working. Removed leftover one-off api-run container. | (verification only) |
| 2026-06-10 | main-assistant | Discovery location-aware ranking: web-search results now sorted by distance to user (nearest first). Backend: `handlers/venue.go` added query params `sort_by_distance` (bool, default true) + `max_distance_m` (int, default 10000); new `filterAndSortPicks()` helper filters by distance + sorts ascending. Frontend: `discover_view.dart` _buildSearchResults() sorts results before display (defense-in-depth, idempotent); `venue_search_service.dart` search() method signature adds `sortByDistance` + `maxDistanceM` params, passes to endpoint. Sidecar already calculates distanceM via haversine in _enrich_coords() — no changes needed. E2E verification pending. See sessions/2026-06-10-discovery-location-aware-ranking.md | anmates-api/handlers/venue.go (import sort, new filterAndSortPicks), anmates_flutter/lib/{views/discover/discover_view.dart, services/venue_search_service.dart} |
| 2026-06-10 | main-assistant | Containerize the LLM — run ALL services in Docker. App services (db/api/ai_venue_search/flutter_web) were already in Docker; only the venue-search structurer LLM ran on the host (LM Studio). Added bundled `ollama` service (`ollama/ollama:latest`, OpenAI API :11434/v1, named `ollama` volume, healthcheck) + one-shot `ollama_pull` (pulls `${OLLAMA_MODEL:-qwen2.5:3b}` ~2GB into the volume, idempotent). Repointed `ai_venue_search`: STRUCTURER default pollinations→openai, LLM_BASE_URL→http://ollama:11434/v1, LLM_MODEL→${OLLAMA_MODEL}, depends_on ollama healthy. New `docker-compose.gpu.yml` opt-in NVIDIA override (kept out of base so `up` never fails CPU-only). start.sh: COMPOSE_FILE select (OLLAMA_GPU=1 merges gpu override), exports OLLAMA_MODEL, 8b model-pull wait loop, banner shows Ollama URL/model. No Python edits — sidecar's existing _lm_studio json_schema path already speaks Ollama's OpenAI shape. Static-verified: compose config -q (base+gpu merge), interpolation resolves, bash -n. PENDING live ./start.sh + json_schema-on-Ollama confirm. See sessions/2026-06-10-dockerize-llm-ollama.md | docker-compose.yml, docker-compose.gpu.yml (new), start.sh, .env, .env.example, sessions/2026-06-10-dockerize-llm-ollama.md |
| 2026-06-10 | main-assistant | Discovery venue thumbnails + Chi tiết quán (12.1) detail screen. (1) NEW public, un-rate-limited Go endpoint `GET /api/v1/venues/image?q=<name>` — keyless DuckDuckGo image search server-side (vqd page scrape → i.js JSON), proxies the photo bytes back (5MiB cap, image/* CT, Cache-Control 24h) so Flutter `Image.network` works with no CORS/token issues; graceful 400/404/502 → placeholder. (2) NEW Flutter `VenueThumbnail` widget (Image.network + fade-in + PhotoSlot fallback) replaces the `📸` placeholder in both Discovery rows (OSM nearby + web-search). (3) NEW `VenueDetailView` (Screen 12.1) — light-theme hero + tag/title/meta + social-proof banner + VỀ QUÁN + taste chips + "Hợp gu {name}: %" match card + sticky "＋ Wishlist / Tìm Mate ăn cùng" bar; tapping any Discovery row now opens it (was: Google Maps). Honesty: meta shows only real segments (rating/price only when present, else distance), social count softened (no fake "15 người"), match % is a deterministic placeholder until vibe-match backend. CTAs wired: Wishlist→WishlistService.add(category mapped), Tìm Mate→SwipeView, near_me→MapsLauncher. Verified: go build/vet/test GREEN (golang:1.25, new parse tests), flutter analyze clean on changed files. PENDING live ./start.sh user verify. Limitation: DDG may block datacenter IPs (Cloud Run) → rows keep placeholder there. See sessions/2026-06-10-venue-thumbnails-and-detail-screen.md | anmates-api/services/venue_image.go (new) + venue_image_test.go (new), anmates-api/handlers/venue_image.go (new), anmates-api/main.go, anmates_flutter/lib/services/api_client.dart, anmates_flutter/lib/widgets/venue_thumbnail.dart (new), anmates_flutter/lib/views/discover/venue_detail_view.dart (new), anmates_flutter/lib/views/discover/discover_view.dart |
| 2026-06-11 | main-assistant | Discovery: make exact-named venue search reliable ("lẩu bò giáo toàn") + crawl photos off the venue's web pages for the detail gallery. **Part 1 (sidecar service.py)**: free-text `/search` now (a) wraps the structurer so an LLM failure degrades to empty picks not a 502, (b) injects a DETERMINISTIC seed card for the typed name when it looks like a venue name (≥2 words / ≥8 chars; bare dishes like "phở" left to the structurer) AND no structured pick matches it (`_name_matches`), titlecased, reason "Quán bạn vừa tìm", coords 0/0 unless user-loc known + geocode lands in radius. Fixes documented non-determinism (exact-name search returned the quán "once, else other venues") since OSM lacks the venue + web-search/LLM is probabilistic. NEW tests/test_query_seed.py (6). **Part 2 (image crawl)**: DDG i.js BLOCKED here + Bing image search mangles VN name into noise → rewrote `services/venue_image.go` resolver to DDG-Lite WEB search → crawl top result pages → extract og:image + content `<img>.jpg/png/webp` (absolutized, junk-filtered logo/icon/avatar/banner/ads), up to 6/venue, 24h cache. `handlers/venue_image.go` Serve+`i` index, new Count `GET /api/v1/venues/images?q=`→{count}; main.go route. Flutter: api_client.imageUrl(index), NEW venue_image_service.count, VenueThumbnail index param, venue_detail_view hero → swipeable PageView gallery + dots. Verify: go build/vet/test GREEN, sidecar pytest 12/12, flutter analyze clean; network chain validated (DDG-Lite→mia.vn→og:image + lau-bo-giao-toan-*.jpg serve 200 image/jpeg 103KB/260KB). PENDING live ./start.sh user verify. See sessions/2026-06-11-discovery-venue-seed-and-web-image-crawl.md | ai-venue-search/ai_venue_search/service.py + tests/test_query_seed.py (new), anmates-api/services/venue_image.go + venue_image_test.go, anmates-api/handlers/venue_image.go, anmates-api/main.go, anmates_flutter/lib/services/{api_client.dart, venue_image_service.dart (new)}, anmates_flutter/lib/widgets/venue_thumbnail.dart, anmates_flutter/lib/views/discover/venue_detail_view.dart |
| 2026-06-11 | main-assistant | Venue image proxy error chain fixed + Goong venue-freshen added. **Image fix (user-confirmed)**: (401) `app.Get("/api/v1/venues/image")` was registered AFTER `api.Use(jwtMW)` — Fiber v2's catch-all JWT mw guards app-level `/api/v1*` routes too → moved registrations BEFORE the Use; (429) `/api/v1` group rate limiter also caught them → wrapped `rlHandler` to skip `/api/v1/venues/image*`; (404) DDG Lite now serves a bot-CAPTCHA when scraped server-side + OSM admin suffixes over-specify → **rewrote services/venue_image.go DDG page-crawl → Bing Images** (parse `murl` JSON from results HTML), `prepareVenueSearchQuery` strips Phường/Quận/TP. + appends "ảnh". **Goong freshen (pending key)**: new `services/goong.go` GoongClient.Freshen (tier1 autocomplete-by-name→detail, ≤150m match guard; tier2 reverse-geocode fallback; 24h cache; nil-degrades to OSM), `handlers/venue_fresh.go` GET /api/v1/venues/fresh (authed, {} on no-hit), config.GoongAPIKey, .env GOONG_API_KEY (empty=off). Flutter venue_fresh_service.dart + venue_detail_view.dart freshen-on-open (mutable _d, withFresh, refetch photos if name changes). Verified: Go build/vet GREEN + services tests pass (golang:1.25; Bing+Goong parsers). PENDING: real GOONG_API_KEY for live freshen verify; Flutter analyze via Docker. Caveat: Goong has no Google-style nearby-POI search → replacement-name detection weaker than Google Places. See sessions/2026-06-11-venue-image-routing-bing-and-goong-freshen.md | anmates-api/{main.go, config/config.go, services/venue_image.go + venue_image_test.go, services/goong.go (new) + goong_test.go (new), handlers/venue_fresh.go (new)}, anmates_flutter/lib/services/venue_fresh_service.dart (new), anmates_flutter/lib/views/discover/venue_detail_view.dart, .env, .env.example |
| 2026-06-11 | main-assistant | Add Email OTP as a 2nd auth option ALONGSIDE phone OTP (no reCAPTCHA). Passwordless: backend mints a 6-digit code, emails it (Gmail SMTP), verifies, upserts a user by email, issues the existing app JWT pair — no Firebase, no captcha. Phone OTP flow untouched. **Backend (additive)**: migration 012_email_otp.sql (`email_otps` table: code_hash/expires_at/consumed_at/attempts); NEW services/email.go (`EmailSender` iface, `SMTPSender` net/smtp auto-STARTTLS+PLAIN for Gmail 587+App Password, `LogSender` dev fallback); services/auth.go +`SetEmailOTP`/`EmailOTPEnabled` + `RequestEmailOTP` (60s per-email cooldown→crypto/rand 6-digit→sha256 store→send) + `VerifyEmailOTP` (newest unconsumed+unexpired→attempts≤5→hash cmp→consume→upsertEmailUser) + `upsertEmailUser` (find-by-email else INSERT passwordless, name=local-part); errors.go ErrRateLimited; interfaces.go AuthServicer ext; handlers/auth.go RequestEmailOTP(200 always on valid email to avoid enumeration; 429 cooldown)+VerifyEmailOTP(401→IssueTokens); config.go SMTP_*+EMAIL_OTP_* (10m/60s/5); main.go wires sender (real SMTP when SMTP_HOST+USERNAME; else LogSender in DEV_MODE; else disabled) + registers POST /api/v1/auth/email/{request-otp,verify-otp} only when enabled; .env.example documented. **Flutter (additive)**: auth_service.dart requestEmailOtp/verifyEmailOtp; NEW email_input_view.dart + email_otp_view.dart (6-box + custom keypad, same UX as phone OtpView, 90s resend); phone_input_view.dart got "Đăng nhập bằng email" button → EmailInputView (same onAuthenticated). Verified: go build/vet/test GREEN (golang:1.25), flutter analyze clean. PENDING live verify: real SMTP creds (or DEV_MODE log sender) + ./start.sh → email→code→app. When confirmed → R-007/next. See sessions/2026-06-11-email-otp-auth-option.md | anmates-api/{db/migrations/012_email_otp.sql (new), services/email.go (new), services/auth.go, services/errors.go, services/interfaces.go, handlers/auth.go, config/config.go, main.go}, .env.example, anmates_flutter/lib/services/auth_service.dart, anmates_flutter/lib/views/auth/{email_input_view.dart (new), email_otp_view.dart (new), phone_input_view.dart} |
| 2026-06-11 | main-assistant | Branded HTML email for the OTP (was plain text). services/email.go: `EmailSender.Send` now takes (to,subject,textBody,htmlBody); `buildMessage` emits multipart/alternative when html present (text fallback kept) + `encodeHeaderWord` (RFC2047 base64 so UTF-8 Subject/From "ĂnMates" renders) + `randomBoundary`; LogSender/SMTPSender updated. services/auth.go: new `emailOTPHTML(code,mins)` — table-based inline-styled responsive email, brand gradient header (berry #B8336A→#8E1F4D), spaced OTP pill, expiry + security note + footer; RequestEmailOTP sends text+html. Verified go build/vet/test GREEN (golang:1.25). Preview by triggering a fresh OTP. | anmates-api/services/email.go, anmates-api/services/auth.go |
| 2026-06-11 | main-assistant | CI/CD: pass SMTP env to Cloud Run so email OTP works on deployed API. Added 3 `--set-env-vars` to BOTH the prod deploy (cd.go-api.yml) and the dev deploy (ci.go-api.yml deploy-dev job): SMTP_HOST + SMTP_USERNAME from GitHub **vars** (non-sensitive; host defaults to 'smtp.gmail.com' via `vars.SMTP_HOST || ...`), SMTP_PASSWORD from a GitHub **secret** (Gmail App Password). Necessary because gcloud `--set-env-vars` REPLACES the whole env set — omitting them would leave SMTP unconfigured → email-OTP routes disabled (main.go gates on SMTP_HOST+SMTP_USERNAME). User must create vars SMTP_HOST + SMTP_USERNAME and secret SMTP_PASSWORD. SMTP_FROM defaults to username, SMTP_FROM_NAME defaults to "ĂnMates" in config → these 3 suffice. | .github/workflows/cd.go-api.yml, .github/workflows/ci.go-api.yml |
| 2026-06-11 | main-assistant | Fix golangci-lint v2.12.2 failures blocking CI (11 issues → 0). From email-OTP code: main.go if-else-chain→switch (gocritic ifElseChain), auth.go randomNumericCode `max`→`upper` (gocritic builtinShadow). Pre-existing on this branch (venue work): handlers/venue.go `sort` local→`sortParam` (gocritic importShadow), handlers+services/venue_image.go `nil` request body→`http.NoBody` ×4 (gocritic httpNoBody) + `defer resp.Body.Close()` → add `//nolint:errcheck` ×4 (errcheck), matching the existing convention in services/auth.go. Verified: `golangci-lint run` (v2.12.2 in docker) → 0 issues; go build + test GREEN. User chose to KEEP Bing as the venue image source (declined Google switch — server-side Google scraping would hit the same CAPTCHA/429 wall that retired DDG). | anmates-api/{main.go, services/auth.go, services/venue_image.go, handlers/venue_image.go, handlers/venue.go} |
| 2026-06-11 | main-assistant | CI/CD: inject TOMTOM_API_KEY from a GitHub **secret** into Cloud Run. Added `--set-env-vars "TOMTOM_API_KEY=${{ secrets.TOMTOM_API_KEY }}"` to BOTH the prod deploy (cd.go-api.yml) and dev deploy (ci.go-api.yml deploy-dev). It's a secret (not a var) since it's a private API key. gcloud `--set-env-vars` REPLACES the whole env set, so the line must be listed alongside the others or it'd be dropped. Backend reads it at runtime (config.go `os.Getenv("TOMTOM_API_KEY")` → cfg.TomTomAPIKey → services.NewTomTomClient); empty/unset degrades gracefully (Enabled()==false → /venues/nearby route not registered → Flutter falls back to Overpass), so it's safe even before the secret exists. Also documented TOMTOM_API_KEY (+ previously-missing SMTP_PASSWORD) in CI-CD.md secrets table. User must create the GH repo secret TOMTOM_API_KEY for the proxy to turn on. PENDING: create secret + next deploy. | .github/workflows/cd.go-api.yml, .github/workflows/ci.go-api.yml, .github/CI-CD.md |
| 2026-06-11 | main-assistant | Port google-maps-scraper venue-enrichment features into Discovery (A/B/C; D=spike no-build). Google Maps source itself is banned in VN → ported the *concepts* fed by permitted sources. **A** "Đang mở/Đã đóng" badge: new utils/opening_hours.dart OSM hours parser (24/7, multi-rule, split shifts, overnight) + widgets/open_now_badge.dart, wired into discover row + detail (replaces raw 🕒 chip; _matchChecks now real). **B** ratings+reviews: new services/venue_reviews.go (keyless Bing web scrape → rating/count/snippets, cached, never fabricated) + handlers/venue_reviews.go GET /api/v1/venues/reviews (auth) + Flutter venue_reviews_service.dart + detail "CẢM NHẬN TỪ CỘNG ĐỒNG" section + merged ⭐ in meta. **C** richer list: places_service.dart getNearby now fetches TomTom+Overpass concurrently and merges via new mergeNearbyPlaces/normalizeVenueName/OsmPlace.mergeFill (dedup by name+250m, fill blank fields, append uniques). **D** popular-times: no VN-legal free source (Google-derived banned; BestTime/Foursquare paid+sparse) → intentionally not built. Images-belong-to-location (user note): already correct via R-007 name+address query + relevance filter. Verified: Go build/vet/test GREEN (golang:1.25, Docker), Flutter analyze clean on changed files + 11/11 new tests. PENDING live confirm via ./start.sh. See sessions/2026-06-11-discovery-scraper-features-port.md | anmates-api/{services/venue_reviews.go,venue_reviews_test.go,handlers/venue_reviews.go,main.go}, anmates_flutter/lib/{utils/opening_hours.dart,widgets/open_now_badge.dart,services/venue_reviews_service.dart,services/places_service.dart,views/discover/venue_detail_view.dart,views/discover/discover_view.dart}, anmates_flutter/test/{opening_hours_test.dart,places_merge_test.dart} |
| 2026-06-11 | main-assistant | Fix 2 golangci-lint (gocritic) failures in services/venue_reviews.go blocking CI. (1) regexpSimplify: count-regex `([\d][\d.,]*)` → `(\d[\d.,]*)` on both countRes patterns (only line 166 was flagged; fixed line 165 too for consistency — equivalent). (2) unnamedResult: parseRatingAndCount return changed to named `(rating float64, count int)`; dropped the now-redundant local `var rating`/`var count` declarations and switched to a bare `return`. Verified: `golangci-lint run` (v2.12.2 in docker) → 0 issues; `go build ./... && go vet ./services/` GREEN (golang:1.25). | anmates-api/services/venue_reviews.go |
| 2026-06-11 | main-assistant | Agentic venue enrichment — realtime Google crawl + LLM-verified photos; image cache OFF. Root cause of the "Surgeon Bbq Curry → operating-room photo" bug: the Bing path's string-match relevance filter (token "surgeon" matches surgery photos; no semantic notion of "food"). User chose **headless-browser (Playwright)** Google scraping. **Sidecar** (ai-venue-search/): NEW providers/google_scrape.py `GoogleCrawler` (headless Chromium drives google.com web search → fetch venue pages w/ httpx → og:image + content <img> w/ alt; consent + /sorry/ CAPTCHA → empty; **Bing httpx fallback**); NEW enrich.py `VenueEnricher` (agentic loop: plan_queries → crawl → **LLM verify+extract** strict json_schema over Ollama: is_food_venue + keep_image_indices rejecting non-food + extracted info; reflect/retry; heuristic fallback when LLM down — safe since images sourced only from food pages); schemas Enrich{Request,edImage,edInfo,Response}; app.py POST /enrich (never 502); config ENRICH_*; requirements playwright + Dockerfile chromium; compose ENRICH_* env; tests/test_enrich.py (12). **Go** (anmates-api/): NEW services/venue_enrich.go (client → /enrich, no cache); venue_image.go **cache OFF** (VENUE_IMAGE_CACHE_TTL default 0; removed hardcoded 24h) + **IsPublicHTTPImageURL** SSRF guard (ctx resolver, blocks loopback/RFC1918/link-local/ULA/metadata); handlers/venue_image.go **?u=<b64url remote> proxy mode** (SSRF-guarded, realtime/consistent) alongside ?q=&i=; NEW handlers/venue_enrich.go GET /api/v1/venues/enrich (public, degrades empty); main.go register + wire AISearchURL; test TestIsPublicHTTPImageURL. **Flutter** (anmates_flutter/): api_client.imageProxyUrl (base64url-unpadded); NEW venue_enrich_service.dart (VenueEnrichment/Info); venue_thumbnail.dart optional imageUrl; venue_detail_view.dart mutable _d, _loadEnrichment (verified gallery + withEnrichment facts merge, Bing fallback else). Verified: sidecar 35/35 pytest + py_compile; Go build/vet + golangci-lint v2.12.2 0 issues + services/handlers/middleware unit tests GREEN. Flutter NOT analyzed (no host PATH). PENDING ./start.sh live confirm (real venue → relevant photos+facts; fake venue → placeholder) → R-008. See sessions/2026-06-11-agentic-venue-enrichment-google-crawl.md | ai-venue-search/{ai_venue_search/providers/google_scrape.py,enrich.py,schemas.py,app.py,config.py,requirements.txt,Dockerfile,tests/test_enrich.py}, docker-compose.yml, anmates-api/{services/venue_enrich.go,services/venue_image.go,services/venue_image_test.go,handlers/venue_image.go,handlers/venue_enrich.go,main.go}, anmates_flutter/lib/{services/api_client.dart,services/venue_enrich_service.dart,widgets/venue_thumbnail.dart,views/discover/venue_detail_view.dart} |
| 2026-06-12 | main-assistant | Discover vibe chips → real multi-select (union) filters. Were visual-only toggles. OsmPlace +airConditioning/outdoorSeating/stars (parsed from OSM tags in fromJson, carried in mergeFill; additive). discover_view: _activeVibes default {} (was pre-selecting 'Máy lạnh') so initial list stays full; _filteredPlaces gains a union vibe filter; NEW _matchesVibe (Máy lạnh=indoor/AC, Vỉa hè=street/fast_food/outdoor, Khuất hẻm=hẻm/ngõ/alley addr, Sang chảnh=stars/upscale cuisine+name, Ngồi khuya=open-late via reused parseOpeningHours@23:00 + bar/night keywords) + _opensLate; chip onTap re-pages; empty-state copy filter-aware; 🔥 nearest marker gated on no-vibe. Verified LIVE (host Chrome/Playwright, geolocation HCMC D1): 4 screenshots show correct reshaping — all→full, Vỉa hè→Lotteria/KFC, Ngồi khuya→bars+Alibaba(open), Sang chảnh→Refinery/Mimi Ultra Lounge. flutter analyze clean on changed files (5 pre-existing infos untouched), places_merge_test 4/4. Note: rebuilt stale api image (served /venues/image behind JWT) to restore public proxy. PENDING user "ok". See sessions/2026-06-12-discover-vibe-filters.md | anmates_flutter/lib/services/places_service.dart, anmates_flutter/lib/views/discover/discover_view.dart |
| 2026-06-12 | main-assistant | Fix: Lotteria/KFC mis-bucketed under Discover "Vỉa hè". OSM tags A/C chains as amenity=fast_food, which my Vỉa hè heuristic treated as street food. Added _indoorChains denylist + _isIndoorChain in discover_view.dart; Vỉa hè excludes chains (+air_conditioning=yes), Máy lạnh includes them; non-chain fast_food stays Vỉa hè. Verified LIVE (force-recreate flutter_web needed — plain up -d kept old container): 05_viahe_fixed.png (no Lotteria), 06_maylanh.png (Lotteria present). flutter analyze discover_view.dart → No issues found. See sessions/2026-06-12-discover-vibe-filters.md | anmates_flutter/lib/views/discover/discover_view.dart |
| 2026-06-12 | main-assistant | Fix "Discovery thumbnails blank" (user: "sao ba quán đầu không có hình"). Root cause: backend serves the photos fine (count=5, real JPEGs) but VENUE_IMAGE_CACHE_TTL=0 (cache off, from the enrichment realtime work) → every thumbnail did a COLD live Bing scrape (~11.8s measured); newly-filter-revealed venues hadn't finished when captured. Two fixes: (1) .env VENUE_IMAGE_CACHE_TTL=10m → repeat thumbnail fetch 11.8s→0.36s (only the keyless Bing image path; agentic detail enrichment stays realtime). (2) venue_thumbnail.dart loading state moved from loadingBuilder → frameBuilder(frame==null): loadingBuilder never fired during the server-hold (no bytes → loadingProgress null → blank slot, indistinguishable from "no photo"); frame==null covers the whole pending window → shows a berry spinner placeholder. Verified LIVE: cache 11.8s→0.36s; 10_spinner.png (berry spinners in slots) → 11_after.png (photos loaded). flutter analyze venue_thumbnail.dart → No issues. Caveat: first-ever view still cold (~10s; reachability-probe in venue_image.go is a future perf item). See sessions/2026-06-12-discover-vibe-filters.md | anmates_flutter/lib/widgets/venue_thumbnail.dart, .env |
| 2026-06-12 | main-assistant | Backfill venue opening_hours from TomTom (user: "bổ sung thêm từ nguồn khác" for the Đang mở/Đã đóng badge). Investigation: ALL keyless web-text sources are blocked from this env — Bing web scrape returns 0 b_algo (reviews endpoint empty even for Pizza 4P's), and the sidecar crawl (Google headless + Bing httpx) returns 0 pages; only Bing IMAGES works. So mirroring venue_reviews.go would be useless. TomTom (already integrated, VN-legal, not Google Maps) DOES return hours but tomtom.go only had an unused OpeningHours field — never requested/parsed. Fix (Go-only, no Flutter change): tomtom.go Nearby now sends openingHours=nextSevenDays, parses poi.openingHours.timeRanges, and tomtomHoursToOSM() converts them to an OSM-style string ("Mo 09:00-22:00; Sa 21:00-04:00" wrap for overnight bars) — the exact format the Flutter OpenNowBadge already parses. Flows through the existing /venues/nearby → OsmPlace.fromBackend(opening_hours) → mergeNearbyPlaces/mergeFill, so it backfills the list badge with zero UI change. Empty ranges → "" (honest, no badge). Verified: go build+vet GREEN, new tomtom_test.go 6/6 PASS, golangci-lint v2.12.2 → 0 issues. CANNOT demo live: TOMTOM_API_KEY is empty locally (it's a GH deploy secret, also still pending). Activates the moment a (free) key is set in .env. Coverage caveat: TomTom may still lack tiny bars → those stay badgeless (honest). See sessions/2026-06-12-discover-vibe-filters.md | anmates-api/services/tomtom.go, anmates-api/services/tomtom_test.go |
| 2026-06-13 | main-assistant | Switch Discovery nearby list to Goong via a pluggable provider + personalize by onboarding + Goong V2. (1) NEW `services/nearby.go`: `NearbyVenue` (moved from tomtom.go) + `NearbyProvider` interface (Enabled/Name/Nearby incl. keywords) + `NewNearbyProvider` factory (env MAP_PROVIDER: goong|tomtom|auto-prefers-goong) + haversineMeters. (2) NEW `services/goong.go` `GoongClient`: fans out food keywords → Goong **V2** `/v2/place/autocomplete`, dedups, concurrent `/v2/place/detail`, returns ALL venues sorted nearest-first **NO distance filter** (user choice), cap 60, cache keyed lat/lng/radius+keywords; amenity from V2 `types` w/ keyword fallback. (3) NEW `services/food_keywords.go` `FoodKeywordsFromOnboarding(food,vibe,culture)`: maps onboarding slang keys (fwb/ons/419/bx/ox/kr/jp/street/fancy…) → real VN dish keywords (deduped, cap 8) so the list surfaces REAL named venues (Lẩu bò Bé Ba, Bò nướng tảng 5S…) not generic "Nhà Hàng". (4) `handlers/venue_nearby.go` takes NearbyProvider + ProfileLookup(UserService); derives personalized keywords from the JWT user's tags — zero Flutter change. (5) tomtom.go kept (Name() + ignored keywords arg). (6) config MapProvider+GoongAPIKey; main.go factory + NewVenueNearby(provider,userSvc). (7) Flutter places_service.getNearby → provider only, Overpass null-fallback; image thumbnails unchanged (ride on Goong-accurate name+address). (8) env/CI: .env(MAP_PROVIDER=goong), .env.example, cd/ci.go-api.yml (+MAP_PROVIDER var,+GOONG_API_KEY secret, kept TomTom), CI-CD.md. Verified: go build/vet/test GREEN; golangci-lint v2.12.2 → 0; flutter analyze clean (5 pre-existing infos); places_merge_test 4/4; live e2e via Go client → V2 + personalized keywords return 14 real named venues. PENDING user ./start.sh in-app confirm → R-008. See sessions/2026-06-13-goong-nearby-provider.md + docs/specs/2026-06-13-goong-discovery-nearby.md | anmates-api/{services/nearby.go,goong.go,food_keywords.go,tomtom.go,nearby_test.go,goong_test.go,food_keywords_test.go,handlers/venue_nearby.go,config/config.go,main.go}, anmates_flutter/lib/{services/places_service.dart,views/discover/discover_view.dart}, .env, .env.example, .github/workflows/{cd,ci}.go-api.yml, .github/CI-CD.md, docs/specs/2026-06-13-goong-discovery-nearby.md |
| 2026-06-13 | main-assistant | Goong Maptiles → CI/CD: inject GOONG_MAPTILES_KEY (--dart-define) into BOTH flutter-web workflows (cd prod + ci deploy-dev) so the new in-app Goong map (tab "Bản đồ") renders on deploy. Chose GH **secret** (consistent w/ GOONG_API_KEY/TOMTOM_API_KEY, billable). Documented in .env.example + .github/CI-CD.md secrets table. Live-probed: Maptiles key → tiles.goong.io style JSON 200 (VECTOR, raster .png 404). Empty-when-unset → build passes, map blank (Goong-only, no OSM fallback per user). YAML validated. | sessions/2026-06-13-goong-maptiles-ci-cd.md; .github/workflows/{cd,ci}.flutter-web.yml; .env.example; .github/CI-CD.md |
