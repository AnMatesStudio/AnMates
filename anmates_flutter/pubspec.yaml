name: anmates
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # State Management
  flutter_bloc: ^8.1.0

  # Routing
  go_router: ^13.2.0

  # Dependency Injection
  get_it: ^7.6.0
  injectable: ^2.3.0

  # HTTP Client
  dio: ^5.4.0

  # Storage (encrypted tokens + local cache)
  flutter_secure_storage: ^9.0.0
  hive_flutter: ^1.1.0

  # Image Caching
  cached_network_image: ^3.3.0

  # Immutable Models + JSON Codegen
  freezed_annotation: ^2.4.0
  json_annotation: ^4.8.0
  dartz: ^0.10.1

  # Connectivity
  connectivity_plus: ^5.0.0

  # Legacy — to be removed phase-by-phase as views migrate to BLoC + bundled fonts
  # provider: kept until main.dart migrates to BlocProvider (Phase 2)
  # google_fonts: kept until splash_screen + anm_widgets refactor (Phase 1/2 polish)
  # shared_preferences: kept until services/api_client + auth_service replaced by core/network (Phase 2)
  # http: kept until services/api_client replaced by core/network ApiClient (Phase 2)
  provider: ^6.1.2
  google_fonts: ^8.1.0
  shared_preferences: ^2.3.3
  http: ^1.2.2

  # Existing (kept)
  web_socket_channel: ^3.0.1
  flutter_map: ^8.3.0
  latlong2: ^0.9.1
  geolocator: ^14.0.2
  url_launcher: ^6.3.1
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.0
  firebase_auth_platform_interface: ^9.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  # Code Generation
  build_runner: ^2.4.0
  freezed: ^2.4.0
  json_serializable: ^6.7.0

  # Testing
  mocktail: ^1.0.0
  # bloc_test deferred to Phase 2 (when AuthCubit lands) — its transitive
  # test package conflicts with Flutter 3.44's pinned matcher/test_api

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true

  assets:
    - assets/food/
    - assets/avatars/

  # Font bundling: declarations to be re-enabled once TTF files are placed
  # in assets/fonts/ (download from Google Fonts).
  # See PHASE_1_FOUNDATION.md for the list of required weights.
  # Until then, AppTextStyles falls back to GoogleFonts (network fetch).
